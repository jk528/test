# 2026-09-24 工作日志

## JK-temp 全盘整理（清理 + 归类）

用户指令：「整理文件归类一下，去除不必要的文件」。
确认口径：A 类 + B 类全清 / 按主题归类 / 送回收站（后因 C 盘空间不足改为「构建产物永久删 + 小文件回收站」）。

### 结果

- 工作区 9.83 GB → **2.07 GB**，文件数 36,347 → **4,375**
- 回收 7,705.90 MB（构建产物 7,656.50 / 临时 48.77 / 旧版文档 0.63）
- 顶层 14 个目录 → **5 个分组**

### 归类终态

```
阅读器项目\   红楼梦范例 · 彩读分析 · ColorTxt项目分析 · test1.xlsm
语料与文本\   四大名著 · 小说 · 红楼梦基础元素
创作与写作\   同人小说可行性分析
自动化与工具\ 新闻联播 · VBA转js · 输入法相关
环境与配置\   Gitee同步配置 · ERT
```

### 预检否决的两项删除（重要教训）

1. `新闻联播\流程\备份\归档_20260916\` —— 初判为冗余备份，**逐文件全量 MD5 后发现 57/107 份内容不同**，是历史版本快照，撤下不删。
   根因：初版重复检测只比「同尺寸 + 首 1MB 哈希」，漏掉同尺寸不同正文。
2. `红楼梦.txt` / `红楼梦_拆分` 的两份副本 —— 120 文件 MD5 全一致，但**两侧各被不同生态活跃引用**
   （范例侧 ← skill 规范 + 脚本工具 + V3.x 报告；四大名著侧 ← splittxt 输入源 + 其余三部名著的成套结构），保留。

### 已修正的外部依赖

- Windows 计划任务「新闻联播每日总结」→ 指向 `自动化与工具\新闻联播\流程\一键生成\xwlb_report.py`
- 桌面 `红楼梦阅读分析.lnk` / `彩读分析.lnk` → 已重建指向新路径
- `.gitignore` 锚定规则 3 行、52 个文档/脚本内的路径引用（含 18 个 py 的 project_dir、7 个 md 的 file:/// 链接）
- 回扫确认：**零残留旧路径引用**

### 未决事项

- C 盘可用空间仍为 3.13 GB —— 数据移到回收站（6.16 GB），需清空才真正释放
- 两个空壳目录 `.temp\`、`彩读分析\` 被进程占用删不掉，重启后自动可删
- Git 未提交（2,350 D + 1 M），`git add -A` 后 Git 会识别为重命名

### 追加核查：彩读分析 MD 是否丢失（用户问「还能还原吗」）

结论：**MD 一个都没删，无需还原**，只是随目录移动到了 `阅读器项目\彩读分析\`。

- 顶层 md **17 个**完整在位（与原状数量/文件名一致）；含 `.temp\ColorTxt` 下 9 个文档（README/CHANGELOG/docs 等）共 **26 个 / 1,201,841 字节**
- 旧位置 `JK-temp\彩读分析\`：**0 文件**（空壳）
- 回收站（385 条索引）中「彩读分析」相关条目 **0**，`.md` 命中 **0** → 从未进回收站
- 7 个 md 的 mtime 变为 09-24 10:54，系 reorg 脚本**就地改写内部路径引用**（`JK-temp\彩读分析\` → `JK-temp\阅读器项目\彩读分析\`，含 `file:///` 链接），正文主体未变
- 注意：`彩读分析` 实际**在 git 跟踪内**（38 个文件）—— 早前"不在 git"的判断是 PowerShell 解码 git 输出的中文乱码所致，已作废

### 追加核查二：回收站条目解析修正 + 被删 .temp 的可还原性

**发现解析 bug**：`$I` 索引的长度字段按「字符数」处理才正确，此前当作字节数导致**文件名被截断一半**，
进而使「回收站中 彩读分析/四大名著 命中 0」的结论**完全失真**。修正后重扫：

- 回收站总条目 386，其中 **09-24 本次清理 356 条**，`$R` 实体**全部存在**（356/356 可还原）
- `红楼梦范例\app\.temp`：**196 个文件 + 目录条目**均在回收站可还原；**内部无任何 .md**
  （内容为 `.py` 探针脚本 / `.txt` 命令回显 / `.png`·`.bmp` 截图 / `.log` / `.pyc`）
- `输入法相关\.temp`：目录级条目在回收站（2,509,351 B，含 `hlm/sgyy/shz/xyj_baimiao` 白描批次 **67 个 .md**）；
  该目录另有 67 个 .md 存于 git HEAD，双通道可恢复
- `四大名著\通用分析\.temp`：7 文件 + 目录在回收站（含 `bak\拆分文本白描逐回分析_通用任务方法论.orig.md`）
- 教训：**用 `$I` 索引判断"某文件是否可还原"时，必须校验解析出的路径长度合理性**，否则会得出"全部丢失"的错误结论

**git 编码教训**：PowerShell 直接管道 `git ls-tree` 会把 UTF-8 中文路径按 GBK 解码成乱码，
使 `-match "中文关键词"` 全部失配。**应改用 Python `subprocess` + `utf-8` 解码**，或先设 `[Console]::OutputEncoding`。

### 追加核查三：执行还原 `红楼梦范例\app\.temp`

用户确认「还原全部 → 新归类路径」。从回收站 `$R` 实体复制到 `阅读器项目\红楼梦范例\app\.temp`：

- 复制 **233 个文件** + 建 1 个目录，**0 失败**
- 还原后 **233 文件 / 17.77 MB**，与整理报告记载的「17.77 MB / 233 文件」**完全一致**
- 构成：`.txt` 114 / `.py` 64 / `.png` 37 / `.log` 14 / `.tsv` 1 / `.bmp` 1 / `.html` 1 / `.pyc` 1
- 回收站副本**保留未动**（采用 copy 而非 move）

**可复用方法（回收站按原路径还原）**：
1. 遍历 `C:\$Recycle.Bin\*\`，`$I` 与 `$R` 同后缀配对
2. `$I` 解析：`ver=QWORD@0`、`size=QWORD@8`、`FILETIME=QWORD@16`、文件名 = `data[28:].decode('utf-16-le').rstrip('\0')`
   （**不要用 24 处的长度字段**，按字符/字节处理会截断一半）
3. 由原路径取出相对 `JK-temp\` 的部分，做新旧前缀替换，`shutil.copy2` 落到目标
4. 目录条目只 `makedirs` 不 `copytree`（内部文件自带独立条目，否则重复）

其余仍可还原：`输入法相关\.temp`（含 67 个白描批次 .md）、`四大名著\通用分析\.temp`（含 1 个 .orig.md）。

### 追加核查四：还原白描批次到四大名著（用户指定落点）

用户指令：「白描批次 .md + hits.txt 还原到四大名著对应文件夹」。确认口径：**批次 md + hits.txt** → **各书 `X分析\白描批次\`**。

**回收站实体完整**：`输入法相关\.temp` 的 `$R` = `C:\$Recycle.Bin\S-1-5-21-...\$RCPU7BK.temp`，
`os.walk` 实测 **118 文件 / 2,509,351 字节**，与 `$I` 记录 size **一字节不差** → 无需动用 git 兜底。

**实际内容（修正此前"67 个"的粗略说法）**：

| 子目录 | .md | hits.txt | 其他 |
|---|---|---|---|
| `.temp\` 根层 | 3（`xyj_head` / `xyj_tail` / `_xyj_task_brief`） | 1（`hits.txt`） | 39 个 `_*.py` / `_*.txt` 探针 |
| `hlm_baimiao` | 12 | 1 | — |
| `sgyy_baimiao` | 12 | 1 | — |
| `shz_baimiao` | 12 | 1 | — |
| `xyj_baimiao` | 10 | — | 含 `_parts\` 21 |
| `xyj_baimiao.bak` | 5 | — | — |
| **合计** | **75** | **4** | 39 |

**执行结果**：复制 **79 文件**（75 .md + 4 hits.txt），0 冲突、0 失败，逐文件 size + MD5 双校验通过。
回收站原件未动（copy 非 move）。39 个探针脚本**按用户口径保留在回收站**。

**落点结构**：

```
语料与文本\四大名著\
├─ 红楼梦\红楼梦分析\白描批次\      13（12 md + hits.txt）
├─ 三国演义\三国演义分析\白描批次\  13（12 md + hits.txt）
├─ 水浒传\水浒传分析\白描批次\      13（12 md + hits.txt）
└─ 西游记\西游记分析\白描批次\      40
     ├─ batch_01~10.md（10）+ xyj_head/xyj_tail/_xyj_task_brief + hits.txt（14）
     ├─ _parts\  21（a/b 拆分件 + gap）
     └─ _bak\     5（原 xyj_baimiao.bak）
```

- 还原时曾保持原命名（西游记带 `xyj_` 前缀），**当日晚些已按用户要求统一**，见「追加核查五」
- `.bak` 目录改名为 `_bak` 以与 `_parts` 对称

**还原前四部书 `X分析\` 已有成果**（本次批次即其上游中间产物）：
`《X》白描手法逐回解析：120回叙事技巧统合分析.md`（西游记为 100 回），西游记侧另有 `西游记白描手法命中清单_hits.txt`。

### 追加核查五：统一 hits.txt 格式与命名（用户指令「Hit.tXt都仿照红楼梦」）

**四份命中清单当时并不一致**（红楼梦为基准）：

| 文件 | 当时差异 | 处理 |
|---|---|---|
| `红楼梦\hits.txt` | 基准：UTF-8 无 BOM / LF / `N\|手法,手法` | 未动 |
| `三国演义\hits.txt` | 多 UTF-8 BOM + CRLF | 去 BOM、CRLF→LF |
| `水浒传\hits.txt` | 多 UTF-8 BOM + CRLF | 去 BOM、CRLF→LF |
| `西游记\xyj_hits.txt` | 中文式 `第N回：手法、手法` | **100 行全部改写**为 `N\|手法,手法` |

**命名统一**（用户确认）：`xyj_hits.txt`→`hits.txt`、`xyj_head.md`→`head.md`、`xyj_tail.md`→`tail.md`。
修正引用 4 处（西游记分析报告 / tail.md / 还原记录 / 本日志），复扫**零残留**。

**规范依据（重要）**：`四大名著\通用分析\拆分文本白描逐回分析_通用任务方法论.md`
第 149 / 224 行定义 —— `hits.txt  # 全书命中数据，每行：章号|手法A,手法B,…`。
红楼梦那份符合规范，西游记原格式属偏离。**今后凡 hits.txt 一律按此格式。**

**终态**：四部书 `白描批次\` 目录完全同构，各 13 个文件
（`batch_01~10.md` + `head.md` + `tail.md` + `hits.txt`）。
hits.txt 行数：红/三/水各 120，西游 100（回数不同属正常）。

**技术要点**：文件名重命名后，**已扫描出的引用列表里的旧路径会失效**，导致漏改——
本次 `tail.md` 内部引用就因此漏掉一轮，需按**改名后的新路径**再补扫一次。

### 追加核查六：用户并行清理（09-24 11:25–11:43）

用户在本轮任务期间自行删除了一批（均入回收站、R=Y 可还原）：

- `11:25` `四大名著\通用分析\.workbuddy`、`通用分析\常识\.screenshots`
- `11:30` `阅读器项目\红楼梦范例\app\.temp` ← **即上轮还原进去的那份，被再次删除**
- `11:33-11:34` `三国演义\…\三国演义白描分析总结.txt`、`水浒传\…\水浒传白描分析总结.txt`
- `11:35` `西游记\…\西游记白描手法命中清单_hits.txt`（与 hits.txt **MD5 完全相同**，纯副本）
- `11:36` `白描批次\_parts`、`_bak`、`_xyj_task_brief.md`
- `11:43` `新闻联播\流程\归档`
- 另 `11:01-11:05`：`创作与写作`、`环境与配置\ERT\ERT培训.html`、`JK-temp\.temp`、`.git\index.lock`

**新闻联播归档确认无碍**：`xwlb_report.py` L72 `ARCHIVE_ROOT = BASE_DIR/"归档"`（即 `新闻联播\归档`），
L2848 `os.makedirs(month_dir, exist_ok=True)` 自动建月目录 —— 被删的 `流程\归档` 不是任务输出路径，
日报计划任务不受影响。

**教训**：用户会并行操作工作区。动手前应重新读盘核对，不要沿用上一轮的文件清单。

### 追加核查七：test1.xlsm 保留确认

用户指令：「test.xlsm 我需要保留」。

- 现位置：`阅读器项目\ColorTxt项目分析\test1.xlsm`（5,316,608 B / 5.07 MB，mtime 2026-07-05 13:16 未变）
- **`git hash-object` = `HEAD:test1.xlsm` = `ae63988914d1f4c778d0292eb0b62d1e438afc57`** → 逐字节一致，仅移动未改动
- 魔数 `D0 CF 11 E0 A1 B1 1A E1` = OLE2 复合文档（BIFF8 二进制工作簿容器），非 zip；
  VBA 源码已抽出为同目录 `VBComponents From (test1.xlsm) 260807171641\`
- 全树唯一 xlsm；**回收站内 xlsm 条目 0** → 清空回收站不会波及它
- 同目录另有两份配套分析：`test1.xlsm优化建议.md`、`test1.xlsm功能分析.md`
- 全树引用复扫：6 处命中，全部为叙述性文字，**无悬空的绝对路径引用**

### 追加核查八：test1.xlsm 移回根目录

用户指令：「回到原来的根文件」。

- 执行：复制 → MD5 校验一致 → 删源（3 步走，无丢失窗口）；`copy2` 保留原 mtime
- 结果：`JK-temp\test1.xlsm`（5,316,608 B，mtime 2026-07-05 13:16:35）
- 复核：`git hash-object` = `HEAD:test1.xlsm` = `ae639889…`，`git status` 已无 `D test1.xlsm`
- 源头目录 `阅读器项目\ColorTxt项目分析\` 保留其 6 个文档 + 2 个子目录（与 git 原始结构一致）
- `整理报告_2026-09-24.md` §4 已同步更正，并补记用户并行删除项（创作与写作 / ERT培训.html / 流程\归档）

### 追加核查九：新闻联播计划任务可用性（端到端实测）

用户指令：「检查报告任务计划程序新闻联播总结的脚本还能用吗？」

**结论：完全可用，已实测通过。**

任务配置（`\新闻联播每日总结`，State=Ready）：
- Execute = `Python312\python.exe`；Arguments = `"…\自动化与工具\新闻联播\流程\一键生成\xwlb_report.py"`（新路径 ✅）
- WorkingDirectory = 同一键生成目录 ✅；Daily 05:00，NextRunTime 2026-09-25 05:00
- ⚠ `LastRunTime = 09-24 05:00`、`Result = 0` 是**搬迁前用旧路径**跑出来的，不能当作新路径可用的证据

脚本自身路径解析（`import` 后打印）：
- `SCRIPT_DIR` → `…\新闻联播\流程\一键生成`；`FLOW_DIR` → `…\流程`；`BASE_DIR` → `…\新闻联播`
- `ARCHIVE_ROOT` = `…\新闻联播\归档` ✅；`FETCH_DIR` = `…\流程\分阶段生成方案` ✅
- 模板命中 `…\流程\旧版本\新闻联播总结_去敏感词模板.md`
- 依赖模块 `fetch_xwlb.py` 靠「多候选目录回退」定位，搬迁天然安全
- 脚本内**零**旧路径字面量（`JK-temp` / `C:\Users` / `Documents\这是什么` 命中 0）
- `Python312` 有 `requests`；无需 `chardet`/`bs4`/`lxml`（脚本未导入）

实测：
1. `--dry-run`（默认取昨日 09-23）→ 退出码 0，耗时 ~20s，全 5 步跑通（CNTV 17 分段 / 央视网快讯 国内 8 国际 3 / 齐鲁网 26 条），
   质量自检 **CRITICAL 0 / ERROR 0 / WARNING 0**
2. `20260923 --preview <临时路径>` → 与正式归档 `归档\2026年9月\新闻联播总结_20260923.md`
   **MD5 完全相同**（`605ebcc59d698dea8c41089844d6204b`，40,431 字节）→ 搬迁对结果零影响
3. 归档现有 115 份，落点 `归档\2026年9月\` 已存在，0924/0925 尚未生成

**工具教训**：`& python … *> out.txt` 会把 UTF-8 日志按 GBK 转写致中文全乱码，
应改用 Python `subprocess` 捕获 bytes 后 `.decode("utf-8")`。该经验已补入 skill §五。

### 追加核查十：与 Gitee 远端比对，确认有无文件丢失

用户指令：「对比一下 gitee，我缺失的文件？」

**结论：零丢失。** 129 个"磁盘上没有"的文件全部是主动删除，且 129/129 都在回收站。

- 远端 `refs/heads/main` = `35d99f8` = 本地 HEAD → **本地提交已全部推送**
- 受控文件 2,352 个，四档分类：A 原位 1 / B 已移动 **2,159** / C0 内容被改写 63 / C1 真缺失 **129**
- C1 = 129 全部可还原（用户删 37：创作与写作 34 + ERT培训.html 1 + 三/水白描总结 2；整理删 92）
- 另发现 `refs/remotes/origin/main` 是**陈旧引用**（指向 7 月 f6e81f2），不影响数据，可 fetch 修正

**踩坑一（严重）**：`core.autocrlf = true`，手工 `sha1("blob %d\0"+content)` 未过过滤器，
**虚报 726 个文件"内容有差异"**。必须用 `git hash-object`（可用 `-- <f1> <f2> …` 批量）。

**踩坑二（严重）**：回收站 `$I` 解出的路径**尾部带 `\x00`**，肉眼不可见，
使 `endswith`/`==` **全部静默失配** → 一度误判"129 个全部查无记录"。
另有副作用：含 NUL 的文本文件被 Read 工具报 **"binary file" 拒读**。

**踩坑三**：删整个文件夹时回收站**只记一条目录级记录**，子文件不单独出现；
且"归类后删除"的记录用的是**新路径**，需叠上归类映射才能匹配旧路径。

结论报告：`JK-temp\Gitee缺失比对报告_2026-09-24.md`
