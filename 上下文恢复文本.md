# Git 双平台同步 - 上下文恢复文本

> 新对话中，将以下全部内容发送给我，说"按此配置执行"，即可恢复所有同步能力。

---

## 一、账号与认证

### Gitee
- 用户名: `jk528528`
- Token: `<你的Gitee_Token>`
- 权限: projects（仓库读写）

### GitHub
- 用户名: `jk528`
- Token: `<你的GitHub_Token>`
- Token 类型: Fine-grained PAT
- 权限: Contents Read and Write（仅限 test 仓库）

### Git 全局配置
```bash
git config --global user.email "user@example.com"
git config --global user.name "User"
```

---

## 二、仓库拓扑

```
本地 JK-temp (main)
    ├── gitee-test → Gitee jk528528/test (main)
    └── github     → GitHub jk528/test (main)
```

### 远程 URL（含认证）
```
gitee-test: https://jk528528:<你的Gitee_Token>@gitee.com/jk528528/test.git
github:     https://jk528:<你的GitHub_Token>@github.com/jk528/test.git
```

### 仓库网页地址
- Gitee: https://gitee.com/jk528528/test
- GitHub: https://github.com/jk528/test

---

## 三、同步模式（口令）

### 模式一："PC端同步"
以本地 JK-temp 为标准，推送到双平台。
```
检查本地有无新文件/修改
  ├── 有 → git add → git commit → push gitee-test + push github
  └── 没有 → 告知"三端已一致，无需操作"
```

### 模式二："网页同步"
以 Gitee 为标准，同步到本地和 GitHub。
```
fetch Gitee → pull 到本地 → push 到 GitHub
```

### 其他口令
| 口令 | 执行内容 |
|------|---------|
| "PC端同步" | 本地有改动 → 提交并推送到 Gitee + GitHub |
| "网页同步" | Gitee 拉取到本地 → 推送到 GitHub |
| "同步一下" | 等同于"PC端同步" |
| "上传xxx" | 将文件复制到 JK-temp → 提交 → 推送到双平台 |
| "查看状态" | 对比三端 commit，显示是否有差异 |

---

## 四、一键恢复命令（新对话执行）

### 步骤1: 认证 GitHub CLI
```bash
echo "<你的GitHub_Token>" | gh auth login --with-token
```

### 步骤2: 验证远程配置
```bash
cd JK-temp
git remote -v
```

### 步骤3: 如需重新配置远程
```bash
cd JK-temp
git remote remove gitee-test
git remote remove github
git remote add gitee-test https://jk528528:<你的Gitee_Token>@gitee.com/jk528528/test.git
git remote add github https://jk528:<你的GitHub_Token>@github.com/jk528/test.git
```

---

## 五、常用操作速查

### PC端同步（本地 → 双平台）
```bash
cd JK-temp
git add .
git commit -m "添加: 文件说明"
git push gitee-test main:main
git push github main:main
```

### 网页同步（Gitee → 本地 → GitHub）
```bash
cd JK-temp
git pull gitee-test main
git push github main:main
```

### 上传 TXT 文件（自动压缩防乱码）
```bash
cd JK-temp
Compress-Archive -Path "文件名.txt" -DestinationPath "文件名.zip" -Force
git add .
git commit -m "添加: 文件说明"
git push gitee-test main:main
git push github main:main
```

### 查看三端状态
```bash
cd JK-temp
git status
git log --oneline -5
```

---

## 六、Gitee API 操作

### 创建新仓库
```powershell
$headers = @{ "Content-Type" = "application/json" }
$body = @{ access_token = "<你的Gitee_Token>"; name = "仓库名"; description = "描述"; private = $false } | ConvertTo-Json
Invoke-RestMethod -Uri "https://gitee.com/api/v5/user/repos" -Method POST -Headers $headers -Body $body
```

### 修改仓库为公开
```powershell
$headers = @{ "Content-Type" = "application/json" }
$body = @{ access_token = "<你的Gitee_Token>"; name = "仓库名"; private = $false } | ConvertTo-Json
Invoke-RestMethod -Uri "https://gitee.com/api/v5/repos/jk528528/仓库名" -Method PATCH -Headers $headers -Body $body
```

### 修改默认分支
```powershell
$headers = @{ "Content-Type" = "application/json" }
$body = @{ access_token = "<你的Gitee_Token>"; name = "仓库名"; default_branch = "main" } | ConvertTo-Json
Invoke-RestMethod -Uri "https://gitee.com/api/v5/repos/jk528528/仓库名" -Method PATCH -Headers $headers -Body $body
```

---

## 七、注意事项

1. **同步标准**: 以本地为主，本地改了什么就同步什么到线上
2. **TXT 乱码**: 中文 Windows 用 GBK 编码，GitHub/Gitee 网页用 UTF-8，建议压缩为 ZIP
3. **GitHub Token 权限**: 当前 Token 仅限 test 仓库的 Contents 读写，无法创建新仓库
4. **Gitee 仓库默认分支**: 新建仓库默认是 `master`，需手动改为 `main`
5. **强制推送**: `--force` 会覆盖远程，谨慎使用
6. **GitHub 网络不稳定**: 如果推送失败，稍等几秒重试即可
7. **Token 安全**: 本文件中的 Token 已替换为占位符，真实 Token 请自行填入后使用
