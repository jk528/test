# 2026-09-16 工作日志

## 设计方案 V2.0 升级（实施步骤 + 工具地址）

**任务**：读完全部 MD 规范文档与 V1.0 设计方案，把规划层文档升级为「可执行施工图」。

**产出**
- `红楼梦文本分析软件设计方案_V2.0.html`（109 KB，86,655 字符，标签配对已校验）
  - 新增第五章「逐模块具体实施步骤」：S0–S8 共 9 节、23 个施工步骤
  - 每步统一六要素：目标与输入输出 / 选型与理由 / 安装命令 / 核心代码 / 验收标准 / 官方链接
  - 新增附录 A「外部资源地址总表」（分词 / 词典 / 前后端 / 导出 四类，60+ 链接）
  - 新增附录 B「依赖清单与一键安装」（requirements.txt + .env 模板）
  - 修正 V1.0 的章节编号重复问题（原有两个「三、」）
- `软件实施步骤_具体方案.md`：同内容的 Markdown 版，可直接进 skill 文档体系

**关键设计决策（后续沿用）**
- **分词锁定 jieba**，不作替换。理由：120 份历史分词产物口径已锁定；pkuseg/HanLP 只作补充分析工具。jieba-fast 接口兼容，仅在性能不足时启用。
- **情感分析以项目内置双引擎为主**（`基础/emotion_analysis.py`），非 cnsenti。理由：cnsenti 无否定翻转与程度加权，本项目引擎更强，且历史 120 章产物基于它。
- **S7 索引走「结构化输出优先、正则提取兜底」**：LLM 直接返回 JSON + Pydantic 校验为主路径，`extract_v34_v2.py` 降级为历史报告兜底。
- **词库扩展只做并集、不覆盖**，避免改变已发布报告口径。
- **推进节奏**：先打通零依赖的 S1–S3，再啃 S4（唯一中改造模块，价值中枢）。

**重点示例（用户点名要求）**：分词模块给出 jieba GitHub / PyPI / 文档 / 大词典 四个地址、`load_userdict` 词典格式示例、`jieba.initialize()` 只初始化一次的性能提示、验收口径（「宝玉」不得被切成「宝/玉」）+ 古白话未登录词巡检。

## 设计方案改为 Markdown 版（V2.0）

**任务**：把 `红楼梦文本分析软件设计方案_V2.0.html` 整体转成 Markdown。

**产出**：`红楼梦文本分析软件设计方案_V2.0.md`（68.5 KB，44,913 字符，1,305 行）
- 章节结构与 HTML 版完全一致：一~八章 + 第五章 S0–S8 实施步骤 + 5.9 工作台 + 附录 A/B + 数据来源
- 结构校验：代码围栏 50 个（配对）、标题 74 个、表格 60 个、外部链接 156 条
- Markdown 化处理：stat 卡片 → 表格；callout → 引用块（带粗体小标题）；链接统一 `<url>` 角标形式
- **注意**：原 HTML 的 `analyze.py` 提示词里有 ` ```json ` 字面量，直接转 MD 会提前闭合代码块，已改写为「输出一个 json 代码块」表述。以后做 HTML→MD 转换须先排查代码块内的三反引号。

**环境坑（重要）**：本机 Bash 工具不可用（`ls`/`dirname` 均 command not found，shim 报 `cd: null directory`）。替代方案：
- 列目录/统计改用 PowerShell 工具
- 跑脚本用绝对路径托管 Python：`C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe -c "..."`（该方式可用，stderr 会报警告但 exit 0）

## 待办 / 后续
- 若要把设计方案作为 skill 沉淀，建议把「S0–S8 实施步骤」拆为独立 skill（当前已在 MD 中成型）。
- HTML 版 V2.0 保留未删；如需统一口径，可以 MD 版为准。
