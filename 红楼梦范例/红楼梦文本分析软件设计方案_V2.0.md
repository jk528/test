# 红楼梦范例全量分析 · 文本分析软件设计方案（V2.0 实施版）

> **版本**：V2.0 · 实施版（Markdown）
> **日期**：2026-09-16
> **分析对象**：`红楼梦范例` 目录（全部文件）
> **定位**：在 V1.0 架构设计基础上，为每个模块补齐**可执行的实施步骤** —— 选型理由 / 安装命令 / 核心代码 / 验收标准 / 官方地址链接
> **配套文件**：`软件实施步骤_具体方案.md`（本方案第五章的独立抽取版，便于嵌入 skill 文档体系）

---

## 目录

- [一、范例全貌盘点（数据盘点结果）](#一范例全貌盘点数据盘点结果)
  - [1.1 分析报告资产与版本迭代](#11-分析报告资产与版本迭代)
- [二、现有机制剖析：四层可复用体系](#二现有机制剖析四层可复用体系)
- [三、软件化可行性评估](#三软件化可行性评估)
- [四、软件架构设计](#四软件架构设计)
- [五、逐模块具体实施步骤（本版核心）](#五逐模块具体实施步骤本版核心)
  - [S0 环境准备与依赖安装](#s0-环境准备与依赖安装)
  - [S1 章节拆分引擎（M1）](#s1-章节拆分引擎m1)
  - [S2 分词与词频引擎（M2）· 重点示例](#s2-分词与词频引擎m2-重点示例)
  - [S3 情感分析引擎（M3）](#s3-情感分析引擎m3)
  - [S4 LLM 深度分析引擎（M4）· 工作量核心](#s4-llm-深度分析引擎m4-工作量核心)
  - [S5 事件挖掘模块（M5）](#s5-事件挖掘模块m5)
  - [S6 人物挖掘模块（M6）](#s6-人物挖掘模块m6)
  - [S7 报告与全局索引引擎（M7）](#s7-报告与全局索引引擎m7)
  - [S8 可视化与导出（M8）](#s8-可视化与导出m8)
  - [5.9 界面工作台设计（6 个）](#59-界面工作台设计6-个)
- [六、技术选型（附官方地址）](#六技术选型附官方地址)
- [七、实施路线图（里程碑 + 验收物）](#七实施路线图里程碑--验收物)
- [八、风险与对策](#八风险与对策)
- [九、打包与分发（单安装包体积预算 + 配置骨架）](#九打包与分发单安装包体积预算--配置骨架)
  - [9.1 结论先行：四种形态的体积](#91-结论先行四种形态的体积)
  - [9.2 实测依据（可复核）](#92-实测依据可复核)
  - [9.3 三个省体积的杠杆（按收益排序）](#93-三个省体积的杠杆按收益排序)
  - [9.4 推荐落点](#94-推荐落点)
  - [9.5 打包配置骨架](#95-打包配置骨架)
  - [9.6 分发前自检清单（验收标准）](#96-分发前自检清单验收标准)
  - [9.7 许可与合规](#97-许可与合规)
  - [9.8 持续集成骨架（可选）](#98-持续集成骨架可选)
  - [9.9 本章与其他章节的对应](#99-本章与其他章节的对应)
- [附录 A 外部资源地址总表](#附录-a-外部资源地址总表)
- [附录 B 依赖清单与一键安装](#附录-b-依赖清单与一键安装)
- [主要数据来源](#主要数据来源)

---

> **V2.0 相对 V1.0 的增量**
>
> V1.0 已完成「资产盘点 → 机制剖析 → 架构设计 → 路线图」的规划层工作；V2.0 把这套规划落成「能照着做」的施工图 —— 新增**第五章 逐模块实施步骤**（8 个模块 × 6 要素：目标 / 选型 / 安装 / 代码 / 验收 / 链接），并新增**附录 A 外部资源地址总表**与**附录 B 依赖清单**。
>
> 以分词为例，V1.0 只写了「jieba 分词 + 用户词典 + 停用词过滤」；V2.0 给出：jieba 官方仓库与 PyPI 地址、安装命令、用户词典文件格式、精确模式的调用代码、备选加速方案（jieba-fast）、繁体词典地址、验收口径（「宝玉」不得被切成「宝 / 玉」）。
>
> 再补**第九章 打包与分发**：回答「做成一个安装包多大」—— 给出实测体积预算（推荐形态安装包约 55 MB，优化后约 45 MB）、三个省体积杠杆（ECharts 词云 / jieba 瘦身 / Tauri 替 Electron），以及 PyInstaller spec、Tauri 配置、Inno Setup 与一键构建脚本的完整骨架。

---

# 一、范例全貌盘点（数据盘点结果）

| 指标 | 数量 | 说明 |
|---|---|---|
| 120 | 章节拆分文件 | `红楼梦_拆分/` |
| 240+ | 分词结果文件 | JSON + TXT 双格式 |
| 120+ | 逐章情感分析 JSON | `情感分析结果/` |
| 260 | 去重人物 | 宽口径，含间接提及 |

「红楼梦范例」目录是一套「数据资产 + 处理脚本 + 提示词规范 + 分析产物」四位一体的**完整文本分析工作集**。全目录按 11 个一级目录/文件组织：

| 目录 / 文件 | 内容说明 | 规模 |
|---|---|---|
| 红楼梦.txt | 全书原文 | 1 份全文（约 2.57 MB） |
| 红楼梦_拆分/ | 按章拆分原文，命名「章号_字数_章名.txt」，段落以全角空格开头 | 120 份 |
| 红楼梦_分词结果/ | jieba 分词后的 JSON 词列表 | 120 份 |
| 红楼梦_分词结果_txt/ | 分词结果的 TXT 视图 | 120 份 |
| 情感分析结果/ | 逐章情绪 JSON（DUTIR 七类 + Hownet 极性 + 高频词） | 120+ 份 |
| 基础/ | 情感词典（DUTIR 27,414 词 / Hownet 19,472 词）、停用词、否定词、递进词、人物名表、分词词典 | 9 文件 + 3 子目录 |
| 情感词库资源统合/ | 5 个公开情感词典资源调研 + cnsenti/cntext 评估 + 扩展建议 | 8 份调研文档 |
| skill/ | 提示词工程规范体系：任务说明书、流程 v2.0、模板 V3.4、归档规范、前置条件文档 | 约 23 份文档 |
| 脚本工具/ | 6 大类自动化脚本（分析生成 / 验证检查 / 升级迁移 / 修复维护 / 人物档案 / 索引归档） | 38 个 .py |
| 分析结果/ | V3.0/V3.2/V3.3/V3.4 各版本逐章报告 + 全局归档索引（7 大部分）+ 结构化提取 JSON | 267 份 MD + 4 JSON |
| 红楼梦_V3.3_全章分析报告存档.zip | V3.3 报告打包存档 | 1 份 |

## 1.1 分析报告资产与版本迭代

| 版本 | 覆盖章节 | 框架特点 |
|---|---|---|
| V3.0 | 1-18 章 | 双轨六要素基础框架 |
| V3.2 | 1-19 章 | 增加冲突动机 / 叙事手法 / 情感趋势（七类情绪分析） |
| V3.3 | 1-80 章 | 归档索引细化（逐章统计、人物档案、伏笔线索） |
| V3.4 | 1-80 + 81-120 章 | 事件颗粒度细化、字段格式统一、DUTIR 七类情绪回填、预言人物表 |

全局归档索引 **`_归档索引_V3.4.md`** 共 1200+ 行，包含 7 大部分：归档索引表、人物出场档案、逐章累计统计、伏笔线索追踪、七类情绪分析汇总、情感基调演变趋势、全局统计摘要。[^1]

---

# 二、现有机制剖析：四层可复用体系

整个范例事实上已经是一套「人工编排 + 脚本辅助 + LLM 生成」的半自动流水线，抽象为四层，每一层都已有可复用实现或规范 —— 这正是软件化的基础。

| 层 | 名称 | 内涵 |
|---|---|---|
| 层 1 | **数据资产** | DUTIR 七类情绪词、Hownet 极性词、停用词、否定词、程度词、人物名表、分词词典、同义/反义词表 |
| 层 2 | **预处理管线** | 章节拆分、jieba 分词、双词典情感统计，全部已有可用 Python 脚本（无第三方依赖） |
| 层 3 | **深度分析** | 「双轨六要素 V3.4」九大模块模板 + 流程规范 + 任务说明书，驱动 LLM 生成逐章分析 |
| 层 4 | **归集索引** | 从 MD 报告提取结构化数据（JSON）→ 人物归并 / 基调归一 → 生成 7 部分全局索引 |

## 2.1 层 1：数据资产（可直接随软件分发）

| 资源 | 规模 | 用途 |
|---|---|---|
| 情感词典_DUTIR | 27,414 词 | 好 / 乐 / 哀 / 怒 / 惧 / 恶 / 惊 七类情绪词，用于七类情绪分布统计 |
| 情感词典_Hownet | 19,472 词 | 正 / 负极性词 + 程度副词（very/extreme/more/ish）+ deny 否定词 |
| 停用词库 | 3,884 词 | 过滤虚词，保证高频词统计质量（4 表并集 + 主表） |
| 否定词.txt | 1,517 词 | 否定翻转规则（奇数次翻转、偶数次还原） |
| 递进词.txt | 28 词 | 程度副词加权（默认权重 1.5） |
| 红楼梦人物名.txt | 362 行 | 人物识别名单（直接出场 + 间接提及） |
| 红楼梦分词词典.txt | 663 行 | jieba 自定义用户词典（专名、人名、词语） |
| 同义词 / 反义词 | 17,817 词群 / 107,934 词条（同义词）；18,797 反义对 | 同义词词林扩展版 / 反义词对表（非情感词典，仅作规模统计） |

## 2.2 层 2：预处理管线（可直接复用）

1. **① 章节拆分** —— 原文按「第X章」标题拆分为 120 个文件；文件名携带元数据：`081_06893_第81章　占旺相四美钓游鱼　奉严词两番入家塾.txt`；段落以全角空格开头，末行为「(本章完)」。
2. **② jieba 分词** —— `jieba.load_userdict(红楼梦分词词典)` + 停用词过滤，输出 JSON 词列表（120 份）与 TXT 视图（120 份）。
3. **③ 双轨情感统计** —— EmotionAnalyzer（DUTIR 七类计数与占比） + SentimentAnalyzer（Hownet 极性 + 否定词翻转 + 程度副词加权，窗口 = 5），输出 `{章号}_emotion.json`。[^2]

## 2.3 层 3：LLM 深度分析机制

| 文档 | 作用 |
|---|---|
| V3.4分析任务说明书.md | LLM 直接调用的「任务书」：八大部分，含章节简称表、事件要求（≥字数÷500）、情感数据取数规则、预言人物表 |
| 红楼梦专用版_前置条件与生成规范.md | 76 KB 完整规范：路径参数、九大模块规则、字段格式、质量阈值、模型调用流程、批处理示例、§13.8 完整数据管线一键脚本、叙事阶段预分类表 |
| 通用版_前置条件与生成规范.md | 去红楼化的通用版本（可复用于任意长文本） |
| 章节内容分析流程.md | 流程 v2.0：事件拆分颗粒度规则、A 轨提取规则、B 轨六要素、情感词三维统计、否定翻转规则、双轨交叉校验、归档索引维护流程 |
| 章节分析空白模板.md | V3.4 九大模块空模板（41 个三级标题），作为每次生成的格式基准 |
| 章节分析归档规范.md | 归档 v1.3：索引表 / 人物档案 / 统计 / 伏笔 / 情绪 / 趋势 7 部分规范 |

> **关键洞察**
>
> 逐章分析报告（V3.4）的九大模块 = 软件内「分析任务」的目标 Schema：
> §一 版本定位 → §二 事件接入清单（事件 ID `E{章}-{序号}`、★/◆/◇ 三级、段落索引）→ §三 新闻六要素 A 轨（5W1H）→ §四 叙事六要素 B 轨（人物基准六要素表 11 列）→ §五 情感词三维统计 + 七类情绪 → §六 人物关系 + 伏笔追踪 → §七 占位符统合 → §八 双轨校验与完整性检测 → §九 后续分析建议。

## 2.4 层 4：结构提取与全局索引（已高度软件化）

1. **`extract_v34_v2.py`（结构提取）** —— 从 80 份 MD 报告正则提取字数、情感基调、三维统计、事件数、冲突数、人物出场档案、伏笔数等，容错增强（约数/小数/缺失 fallback），输出 `v34_extract_v2.json`（17,320 行）。[^3]
2. **`gen_index.py`（全局索引）** —— 用 ALIAS 人物别名归并字典（黛玉→林黛玉、凤姐→王熙凤等 40+ 条）+ TONE_MAP 情感基调归一（14 种表述 → 6 标准标签），生成 7 大部分归档索引。[^4]
3. **人物档案 / 验证 / 升级类脚本** —— `extract_all_persons`、`analyze_persons_v2`（别名检测/去重/频次）；`verify_v32` / `deep_verify` / `verify_alignment` / `check_dicts`；`v34_convert`（V3.3→V3.4 80 章批量升级）。

---

# 三、软件化可行性评估

| 现有资产 | 改造量 | 说明 |
|---|---|---|
| 情感分析双引擎（emotion_analysis.py） | 直接复用（≈0 改造） | 封装为类库，仅需补输出格式与异常处理 |
| jieba 分词 + 词典/停用词 | 直接复用 | 依赖库打包（jieba 体积可接受） |
| 拆分规则（章节标题 / 全角空格） | 低改造 | 改为「正则 + 配置化」引擎，支持任意小说章节格式 |
| 情感词库资产 | 直接随包分发 | 加入「词库热更新 / 扩展」管理界面 |
| LLM 提示词规范（九大模块 / 双轨） | **中改造（核心工作量）** | 封装为：模板引擎 + LLM API 调度 + 任务队列 + 断点续跑 |
| 报告结构提取（extract_v34_v2） | 低改造 | 改为「结构化输出」优先（LLM 直接返回 JSON），正则提取作为 fallback |
| 索引生成（gen_index + ALIAS/TONE_MAP） | 低改造 | ALIAS / TONE_MAP 配置化，可编辑、可导入导出 |
| 校验脚本（verify / deep / check_dicts） | 中改造 | 迁移为「质量门禁」服务，分析完成后自动执行 |

> **结论**
>
> 范例机制已覆盖软件全部核心功能域（拆分 / 情感 / 事件 / 人物 / 索引 / 校验 / 词库），多数模块可直接复用或低改造。主要工程化工作在于「编排」与「界面」。

---

# 四、软件架构设计

## 4.1 总体架构（五层）

- **接入层**：拖拽导入 TXT/MD 全文 → 自动识别章节 → 写入项目库
- **数据层**：SQLite（元数据、任务状态、索引）+ 文件系统（原文、分词、情感 JSON、报告 MD、导出文件）
- **引擎层**：拆分引擎 / 分词引擎 / 情感引擎 / LLM 分析调度引擎 / 提取与索引引擎
- **服务层**：FastAPI 提供 REST API（任务提交、进度、结果、配置）；任务队列 + 断点续跑 + 增量跳过
- **呈现层**：Web 工作台（Vue3 + ECharts）+ 报告阅读器（MD/HTML 渲染 + 导出 Word/PDF）

## 4.2 八大核心模块

| 模块 | 名称 | 职责 |
|---|---|---|
| M1 | 导入与章节拆分引擎 | 章节识别规则（「第X回 / 第X章」）+ 段落切割（全角空格 / 空行）；输出 `{章号}_{字数}_{章名}.txt`，支持任意中文小说 |
| M2 | 分词与词频引擎 | jieba 用户词典（可编辑）+ 停用词过滤（可编辑）；输出分词 JSON/TXT 双格式 + 每章 Top30 高频词 |
| M3 | 情感分析引擎 | EmotionAnalyzer + SentimentAnalyzer：DUTIR 七类 + Hownet 极性 + 否定翻转 + 程度加权；词库可热更新 |
| M4 | LLM 深度分析引擎 | V3.4 任务说明书 + 空白模板封装为提示词模板库；多 LLM 后端；任务队列、并发批量、断点续跑、增量跳过 |
| M5 | 事件挖掘模块 | 事件拆分规则引擎（起因→行动→结果、ID `E{章}-{序号}`、★/◆/◇ 三级、段落索引）→ 5W1H 结构化事件表 → 事件时间线 |
| M6 | 人物挖掘模块 | 人物库（362 行）+ 别名归并（ALIAS 可配置）+ 首次出场/出场次数/人物关系 + 人物情感档案 + 共现网络图 |
| M7 | 报告与全局索引引擎 | LLM 输出 JSON → 双格式渲染（Markdown 报告 + 全局索引 7 部分）；extract 正则提取作为校验/降级；TONE_MAP 基调归一 |
| M8 | 可视化与导出 | 章节情感曲线、事件时间线、人物关系图谱、高频词云；报告导出 MD/HTML/Word/PDF；索引导出 Excel |

## 4.3 完整数据流

```text
原文 TXT
   │  M1 章节拆分（自动识别 + 元数据命名）
   ▼
红楼梦_拆分/{章号}_{字数}_{章名}.txt
   │  M2 jieba 分词（用户词典 + 停用词）
   ▼
红楼梦_分词结果/{章号}.json + _txt
   │  M3 情感分析（DUTIR 七类 + Hownet 极性）
   ▼
情感分析结果/{章号}_emotion.json
   │  M4 LLM 深度分析（九大模块模板 + 任务队列 + 断点）
   ▼
分析结果/{章号}_{简称}_V3.5_分析报告.md（九大模块）
   │  M5 事件挖掘（结构化提取：事件表 + 5W1H + 分级）
   ▼
结构化 JSON（事件 / 人物 / 情绪 / 冲突 / 伏笔）
   │  M6 人物挖掘 + M7 全局索引（ALIAS / TONE_MAP）
   ▼
_归档索引.md（7 大部分）+ 人物档案.json
   │  M8 可视化 & 导出
   ▼
情感曲线 · 关系图谱 · 事件时间线 · 报告 Word/PDF
```

---

# 五、逐模块具体实施步骤（本版核心）

本章把架构图翻译成施工步骤。每个模块统一按六要素展开：**① 目标与输入输出 → ② 选型与理由 → ③ 安装命令 → ④ 核心代码 → ⑤ 验收标准 → ⑥ 官方链接**。

优先级标记：`P0 必做` · `P1 重要` · `P2 增强`

## S0 环境准备与依赖安装

### S0-1 建立隔离的 Python 环境

**目标**：为项目创建独立虚拟环境，避免污染系统 Python。
**要求**：Python ≥ 3.10（项目脚本已在 3.10 验证，`__pycache__` 中存在 `cpython-310.pyc`）。

```bash
# 1) 确认 Python 版本
python --version           # 需 >= 3.10

# 2) 在项目根目录创建虚拟环境
cd "红楼梦范例"
python -m venv .venv

# 3) 激活（Windows / Git Bash）
source .venv/Scripts/activate
#    激活（Windows / CMD 或 PowerShell）
#   .venv\Scripts\activate.bat     或   .venv\Scripts\Activate.ps1

# 4) 升级 pip
python -m pip install --upgrade pip
```

- Python 官网：<https://www.python.org/downloads/>
- venv 文档：<https://docs.python.org/zh-cn/3/library/venv.html>

### S0-2 安装依赖（分三档，按需选择）

说明：层 1-2（拆分/分词/情感）依赖极轻；层 3（LLM）需要一个 API SDK 或本地模型；层 4 后端/前端另计。

```bash
# —— 档 A：预处理管线（必需，仅 1 个库）——
pip install jieba

# —— 档 B：LLM 深度分析与服务端 ——
pip install openai jinja2 pydantic python-dotenv loguru
pip install fastapi "uvicorn[standard]" sqlmodel aiosqlite

# —— 档 C：可视化与导出 ——
pip install networkx pyvis wordcloud pyecharts openpyxl python-docx

# 一键生成锁定清单（便于复现）
pip freeze > requirements.lock.txt
```

> **最大红利**
>
> 项目现有 38 个脚本**零第三方依赖**（仅标准库 `os / re / json / glob / collections / subprocess`），可直接迁移进软件而无需改造依赖 —— 这是本次软件化的最大红利。

### S0-3 目录约定与配置外置

**目标**：把现有脚本里硬编码的绝对路径抽成配置，便于软件打包与多项目复用。

```python
# config.py —— 全部路径集中管理，取代硬编码
import os
ROOT       = os.path.dirname(os.path.abspath(__file__))
SPLIT_DIR  = os.path.join(ROOT, "红楼梦_拆分")         # 输入：章节原文
TOKEN_DIR  = os.path.join(ROOT, "红楼梦_分词结果")     # 输出：分词 JSON
TOKEN_TXT  = os.path.join(ROOT, "红楼梦_分词结果_txt") # 输出：分词 TXT
EMO_DIR    = os.path.join(ROOT, "情感分析结果")         # 输出：情绪 JSON
REPORT_DIR = os.path.join(ROOT, "分析结果")             # 输出：报告 MD
BASE_DIR   = os.path.join(ROOT, "基础")                 # 词库资产
SEG_DICT   = os.path.join(BASE_DIR, "红楼梦分词词典.txt")
STOP_FILES = [os.path.join(BASE_DIR, "停用词.txt")] + \
             [os.path.join(BASE_DIR, "停用词库", f)
              for f in os.listdir(os.path.join(BASE_DIR, "停用词库"))]
```

验收标准：

- [ ] 所有模块均从 `config.py` 取路径，脚本内不再出现绝对路径
- [ ] 配置支持 `.env` 覆盖（便于打包为桌面软件后由用户指定数据目录）

- python-dotenv：<https://github.com/theskumar/python-dotenv>
- loguru 日志：<https://github.com/Delgan/loguru>

## S1 章节拆分引擎（M1）

### S1-1 目标与输入输出 `P0`

- **输入**：整本 `红楼梦.txt`（或任意中文小说 TXT）
- **输出**：`红楼梦_拆分/{章号:03d}_{字数}_{章名}.txt`，共 120 份
- **已锁定口径**：文件名 3 段式；段落以全角空格（U+3000）开头；末行「(本章完)」

### S1-2 选型与理由

| 候选方案 | 能力 | 采用建议 |
|---|---|---|
| 自研正则（re） | 完全可控，零依赖，可覆盖「第X回 / 第X章 / 卷X」 | **首选**。项目既有规则即由此实现，成本最低 |
| cn2an | 「第壹佰贰拾回」→「第120回」，中文数字转阿拉伯数字 | 推荐引入，解决章号排序与文件命名 |
| chardet | 自动探测 TXT 编码（UTF-8/GBK/GB18030） | 推荐引入，解决外部小说导入乱码 |
| ebooklib / mobi | 解析 EPUB / MOBI | P2 扩展，若要支持电子书导入再加 |

- cn2an：<https://github.com/Ailln/cn2an> · <https://pypi.org/project/cn2an/>
- chardet：<https://github.com/chardet/chardet>
- ebooklib：<https://github.com/aerkalov/ebooklib>

### S1-3 安装命令

```bash
pip install cn2an chardet
```

### S1-4 核心代码（可直接运行）

```python
# split_chapters.py —— 通用章节拆分器（配置化正则 + 编码自适配）
import os, re, json
import chardet
import cn2an
from config import ROOT, SPLIT_DIR

# 章节标题正则：第X回 / 第X章 / 卷X（X 支持中文数字与阿拉伯数字）
CHAP_RE = re.compile(
    r'^[　\s]*(第[零一二三四五六七八九十百千两壹贰叁肆伍陆柒捌玖拾0-9]+[回章节卷])'
    r'[　\s]*(?P<title>[^\n]{0,60})$', re.M)

def read_text(path):
    raw = open(path, 'rb').read()
    enc = chardet.detect(raw)['encoding'] or 'utf-8'
    return raw.decode(enc, errors='ignore')

def split_by_title(text, keep_blank=True):
    """按章节标题切分，返回 [(标题, 正文), ...]"""
    marks = list(CHAP_RE.finditer(text))
    if not marks:
        return []
    out = []
    for i, m in enumerate(marks):
        start = m.start()
        end = marks[i + 1].start() if i + 1 < len(marks) else len(text)
        out.append((m.group(0).strip(), text[start:end].strip('\n')))
    return out

def normalize(body):
    """段落归一：统一换行、段首补全角空格、去空白行"""
    body = body.replace('\r\n', '\n').replace('\r', '\n')
    lines = []
    for ln in body.split('\n'):
        ln = ln.strip()
        if not ln:
            continue
        lines.append('　' + ln)          # 段首全角空格（与既有口径一致）
    return '\n'.join(lines) + '\n(本章完)'

def chapter_no(title):
    """从标题提取章号（中文数字 → 阿拉伯数字）"""
    m = re.match(r'^第([零一二三四五六七八九十百千两壹贰叁肆伍陆柒捌玖拾0-9]+)[回章节卷]', title)
    if not m:
        return 0
    s = m.group(1)
    try:
        return int(cn2an.cn2an(s, 'smart'))
    except Exception:
        return int(s) if s.isdigit() else 0

def run(src='红楼梦.txt'):
    text = read_text(os.path.join(ROOT, src))
    chapters = split_by_title(text)
    os.makedirs(SPLIT_DIR, exist_ok=True)
    manifest = []
    for title, body in chapters:
        no = chapter_no(title)
        body = normalize(body)
        name = f"{no:03d}_{len(body)}_{title}.txt"
        with open(os.path.join(SPLIT_DIR, name), 'w', encoding='utf-8') as f:
            f.write(body)
        manifest.append({"no": no, "title": title, "chars": len(body), "file": name})
    with open(os.path.join(ROOT, '拆分清单.json'), 'w', encoding='utf-8') as f:
        json.dump(manifest, f, ensure_ascii=False, indent=2)
    print(f"✅ 共拆分 {len(manifest)} 章")
    return manifest

if __name__ == '__main__':
    run()
```

### S1-5 验收标准

- [ ] 产出 `红楼梦_拆分/` 下正好 **120** 个 .txt（V3.4 口径为 120 回）
- [ ] 文件名严格匹配 `^\d{3}_\d+_.+\.txt$`，章号连续 001–120 无缺号
- [ ] 每章末行为 `(本章完)`；正文段首均为全角空格
- [ ] 与既有文件对照：第 81 章应为 `081_06893_第81章　占旺相四美钓游鱼　奉严词两番入家塾.txt`

风险提示：

- 回目里的全角空格（U+3000）易被编辑器转成半角，需在写文件时显式使用 `'\u3000'`
- 章名若超过 60 字符会被正则截断，Windows 路径长度上限另需注意（建议 `\\?\` 前缀或缩短项目根路径）

## S2 分词与词频引擎（M2）· 重点示例

### S2-1 目标与输入输出 `P0`

- **输入**：`红楼梦_拆分/{章号}_{字数}_{章名}.txt`（120 份）
- **输出**：① `红楼梦_分词结果/{章号}.json`（词列表数组）② `红楼梦_分词结果_txt/{章号}.txt`（分词视图）③ 每章 Top30 高频词（供 M3 复用）
- **既有产物口径**：JSON 为 `list[str]`，直接 `json.dump(words, ensure_ascii=False)`

### S2-2 选型与理由：为什么是 jieba

| 工具 | 类型 | 特点与适用判断 |
|---|---|---|
| **jieba** | 词典 + HMM | **首选。** 纯 Python、MIT 协议、`load_userdict` 可加载项目已有的 663 行红楼梦词典、支持词性标注与关键词抽取；项目 120 份分词结果即由它产出，口径已锁定 |
| jieba-fast | jieba 的 C 加速版 | 接口与 jieba 基本兼容，速度提升数倍；**仅在 120 章全量重跑耗时明显时启用** |
| pkuseg | 领域自适应 | 可针对领域微调，精度可能更高，但对本项目需重跑全部结果、破坏既有口径，**不建议替换** |
| HanLP / LTP / THULAC | NLP 全流程 | 提供分词 + 词性 + NER，对 M6 人物识别、M5 事件要素抽取有增益；适合作为**补充引擎**而非替代 |
| SnowNLP | 轻量中文处理 | 内置情感打分（自训语料，非词典法），可作为 M3 的交叉参考，**不作为主引擎** |

官方地址：

- jieba 仓库：<https://github.com/fxsjy/jieba>
- jieba PyPI：<https://pypi.org/project/jieba/>
- jieba 中文文档：<https://github.com/fxsjy/jieba/blob/master/README.md>
- 繁体 / 大词典：<https://github.com/fxsjy/jieba/raw/master/extra_dict/dict.txt.big>
- 小词典：<https://github.com/fxsjy/jieba/raw/master/extra_dict/dict.txt.small>
- jieba-fast：<https://pypi.org/project/jieba-fast/> · <https://github.com/deepcs233/jieba_fast>
- 其他语言版：cppjieba <https://github.com/yanyiwu/cppjieba> · jieba-analysis(Java) <https://github.com/huaban/jieba-analysis> · gojieba <https://github.com/yanyiwu/gojieba> · nodejieba <https://github.com/yanyiwu/nodejieba>
- 备选引擎：pkuseg <https://github.com/lancopku/pkuseg-python> · HanLP <https://github.com/hankcs/HanLP> · LTP <https://github.com/HIT-SCIR/ltp> · THULAC <https://github.com/thunlp/THULAC-Python> · SnowNLP <https://github.com/isnowfy/snownlp>

### S2-3 安装命令

```bash
# 首选
pip install jieba

# 若需加速（可选，接口兼容）
pip install jieba-fast
#   代码中改为：import jieba_fast as jieba

# 若需领域分词（备选，会改变既有口径，谨慎）
pip install pkuseg
```

### S2-4 用户词典格式（项目已有 663 行，可继续扩写）

- **文件**：`基础/红楼梦分词词典.txt`
- **格式**：一行一词，三段式 `词语 词频 词性`，后两段可省略，用空格分隔、顺序不可颠倒、必须 UTF-8 编码。

```text
# 基础/红楼梦分词词典.txt（示例）
宝玉 100 nz
黛玉 100 nz
凤姐 80 nz
大观园 60 ns
栊翠庵 40 ns
沁芳闸 30 ns
蓼汀花溆 20 ns
冷子兴 20 nr
焙茗 15 nr
好了歌 10 nz
```

```python
# 补充新词（三种方式）
import jieba
jieba.add_word('蓼汀花溆', freq=200, tag='ns')      # 单条追加
jieba.del_word('宝玉')                              # 删除误切
jieba.suggest_freq(('中', '将'), True)              # 调整组合词频，纠正歧义
```

### S2-5 核心代码（精确模式 + 停用词过滤 + 双格式输出）

```python
# tokenize.py —— jieba 分词引擎（完整可运行）
import os, json, glob
from collections import Counter
import jieba

from config import SPLIT_DIR, TOKEN_DIR, TOKEN_TXT, SEG_DICT, STOP_FILES

# ---------- 1. 一次性初始化（务必在循环外调用，避免重复加载词典）----------
jieba.initialize()                       # 手动初始化，1~3 秒
jieba.load_userdict(SEG_DICT)            # 载入红楼梦专名/人名词典
# jieba.set_dictionary('data/dict.txt.big')   # 若原文含繁体，切换大词典

# ---------- 2. 停用词（主表 + 停用词库 4 表并集）----------
def load_stopwords(paths):
    sw = set()
    for p in paths:
        if os.path.isfile(p):
            with open(p, 'r', encoding='utf-8') as f:
                sw.update(l.strip() for l in f if l.strip())
    return sw

STOPWORDS = load_stopwords(STOP_FILES)

# ---------- 3. 单章分词 ----------
def tokenize_text(text):
    """精确模式；过滤空白与纯标点"""
    words = [w.strip() for w in jieba.lcut(text, cut_all=False) if w.strip()]
    return [w for w in words if any('\u4e00' <= c <= '\u9fff' for c in w)]

def tokenize_chapter(path):
    with open(path, 'r', encoding='utf-8') as f:
        text = f.read()
    return tokenize_text(text)

# ---------- 4. 批量跑 120 章 ----------
def run():
    os.makedirs(TOKEN_DIR, exist_ok=True)
    os.makedirs(TOKEN_TXT, exist_ok=True)
    for path in sorted(glob.glob(os.path.join(SPLIT_DIR, '*.txt'))):
        ch = os.path.basename(path)[:3]
        words = tokenize_chapter(path)

        # 输出 JSON（与既有 120 份产物口径一致：list[str]）
        with open(os.path.join(TOKEN_DIR, f'{ch}.json'), 'w', encoding='utf-8') as f:
            json.dump(words, f, ensure_ascii=False)

        # 输出 TXT 视图
        with open(os.path.join(TOKEN_TXT, f'{ch}.txt'), 'w', encoding='utf-8') as f:
            f.write(' '.join(words))

        # Top30 高频词（去停用词后）
        wc = Counter(w for w in words if w not in STOPWORDS)
        top = wc.most_common(30)
        print(f'  {ch}: {len(words)}词   Top3={[w for w, _ in top[:3]]}')
    print('✅ 分词完成')

if __name__ == '__main__':
    run()
```

> **性能提示**
>
> jieba 首次加载词典需 1–3 秒且会生成缓存。若软件以服务方式常驻，应**进程内只初始化一次**；若每次调用独立进程，请设置 `jieba.dt.tmp_dir` 到可写目录，否则受限文件系统下会反复重建缓存。

### S2-6 词性标注与关键词抽取（为 M5/M6 预埋）`P1`

**用途**：A 轨 5W1H 需要识别「谁 / 哪里 / 何时」（对应 nr / ns / nt 词性）；M6 人物识别需要人名候选。

```python
import jieba.posseg as pseg
import jieba.analyse

text = "黛玉在潇湘馆里听宝玉说笑了半日。"

# 词性标注（ictclas 兼容标记）
for w, flag in pseg.cut(text):
    print(w, flag)         # 黛玉/nr 在/p 潇湘馆/ns ...

# 关键词抽取（TF-IDF 与 TextRank 两种）
jieba.analyse.set_stop_words('基础/停用词.txt')
kw_tfidf = jieba.analyse.extract_tags(text, topK=10, withWeight=True)
kw_tr    = jieba.analyse.textrank(text, topK=10, withWeight=True, allowPOS=('ns','n','vn','v'))

# 提取全部人名候选（与 红楼梦人物名.txt 取交集做校验）
persons = {w for w, fl in pseg.cut(text) if fl == 'nr'}
```

- 词性标记说明：<https://github.com/fxsjy/jieba#词性标注>
- TF-IDF / TextRank 示例：<https://github.com/fxsjy/jieba/blob/master/test/extract_tags.py>

### S2-7 验收标准

- [ ] `红楼梦_分词结果/` 与 `红楼梦_分词结果_txt/` 各 **120** 个文件，均非空
- [ ] 抽查专名切分正确：`宝玉` / `黛玉` / `凤姐` / `大观园` / `栊翠庵` 未被打散成单字
- [ ] JSON 可被 `json.load()` 正常解析，元素均为 `str`（`emotion_analysis.py` 的 `_extract_words` 可直接消费）
- [ ] 单章词数与原文字数比例合理（经验值：词数约为字数的 55%–70%），异常章节需人工抽查
- [ ] Top30 高频词中不应出现「的 / 了 / 是 / 一个」等停用词

> **古白话专项风险**
>
> 《红楼梦》为清代古白话，「梯己 / 打谅 / 行动 / 嗔着」等词现代词典覆盖不足。建议增加一道**未登录词巡检**：统计出现频次 ≥ 5 但仍被切成单字的二字串，人工筛出后写入 `红楼梦分词词典.txt` 并重跑该章。

## S3 情感分析引擎（M3）

### S3-1 目标与输入输出 `P0`

- **输入**：`红楼梦_分词结果/{章号}.json`
- **输出**：`情感分析结果/{章号}_emotion.json`，字段：`dutir_{好乐哀怒惧恶惊}`（count/pct/words）、`dutir_total/pos/neg/neu`、`hownet_pos/neg/pct`、`top_words`(30)
- **引擎**：直接复用 `基础/emotion_analysis.py`（EmotionAnalyzer + SentimentAnalyzer，零第三方依赖）

### S3-2 选型与理由

| 方案 | 原理 | 采用建议 |
|---|---|---|
| 项目内置双引擎 | DUTIR 七类 + Hownet 极性 + 否定翻转 + 程度加权 | **首选主引擎。** 已有 120 章产物、口径明确、可解释性强（返回具体匹配词） |
| cnsenti（已并入 cntext） | 词典法，封装 Hownet + DUTIR | **词典来源**可借鉴；注意其**不做否定翻转与程度加权**，项目引擎在这一点上更强 |
| SnowNLP | 自训语料的情感打分 | 作为交叉参考（P2），与词典法口径不同，不宜混用 |
| BERT 类模型 | 预训练语言模型微调 | P2 远期方向；古白话语料微调成本高，且丧失「可解释的词级证据」 |

- cnsenti：<https://github.com/hiDaDeng/cnsenti> · <https://pypi.org/project/cnsenti/>
- cntext（升级版）：<https://github.com/hiDaDeng/cntext>
- DUTIR 词汇本体：<http://ir.dlut.edu.cn/info/1013/1142.htm>
- Hownet 词语集：<http://www.keenage.com/html/c_bulletin_2007.htm>（原站可能失效）

### S3-3 核心代码（复用既有引擎 + 停用词过滤 + 结构化落盘）

```python
# emotion_pipeline.py —— 双轨情感统计（DUTIR 七类 + Hownet 极性）
import os, sys, json, glob
from collections import Counter

from config import BASE_DIR, TOKEN_DIR, EMO_DIR, STOP_FILES
sys.path.insert(0, BASE_DIR)
from emotion_analysis import EmotionAnalyzer, SentimentAnalyzer   # 既有模块

EMOTIONS = ['好', '乐', '哀', '怒', '惧', '恶', '惊']

def load_stopwords(paths):
    sw = set()
    for p in paths:
        if os.path.isfile(p):
            with open(p, 'r', encoding='utf-8') as f:
                sw.update(l.strip() for l in f if l.strip())
    return sw

def run():
    ea = EmotionAnalyzer(dict_dir=os.path.join(BASE_DIR, '情感词典_DUTIR'))
    sa = SentimentAnalyzer(dict_dir=os.path.join(BASE_DIR, '情感词典_Hownet'),
                           negation_file=os.path.join(BASE_DIR, '否定词.txt'),
                           degree_file=os.path.join(BASE_DIR, '递进词.txt'))
    stopwords = load_stopwords(STOP_FILES)
    os.makedirs(EMO_DIR, exist_ok=True)

    for path in sorted(glob.glob(os.path.join(TOKEN_DIR, '*.json'))):
        ch = os.path.basename(path)[:3]
        with open(path, 'r', encoding='utf-8') as f:
            words = [w for w in json.load(f) if w and w not in stopwords]

        emo  = ea.analyze_words(words)
        sent = sa.analyze_words(words)
        wc   = Counter(w for w in words if w)
        total = emo['total_emotion_words']

        out = {"chapter": ch}
        for e in EMOTIONS:
            c = emo['emotion_counts'].get(e, 0)
            out[f'dutir_{e}'] = {
                "count": c,
                "pct": round(c / total * 100, 1) if total else 0,
                "words": ', '.join(emo['emotion_words'].get(e, [])[:15]),
            }
        out.update({
            "dutir_total": total,
            "dutir_pos": emo['positive_emotion_count'],
            "dutir_neg": emo['negative_emotion_count'],
            "dutir_neu": emo['neutral_emotion_count'],
            "hownet_pos": sent['pos_count'],
            "hownet_neg": sent['neg_count'],
            "hownet_pos_pct": sent['pos_percentage'],
            "hownet_neg_pct": sent['neg_percentage'],
            "hownet_total": sent['total_sentiment_words'],
            "top_words": [{"word": w, "count": c} for w, c in wc.most_common(30)],
        })
        with open(os.path.join(EMO_DIR, f'{ch}_emotion.json'), 'w', encoding='utf-8') as f:
            json.dump(out, f, ensure_ascii=False, indent=2)
        print(f"  {ch}: total={total} pos={emo['positive_emotion_count']} neg={emo['negative_emotion_count']}")
    print('✅ 情绪 JSON 生成完成')

if __name__ == '__main__':
    run()
```

**核心算法口径（务必与既有报告保持一致）**：否定词在窗口 = 5 的范围内**奇数次翻转、偶数次还原**；程度副词权重：`very=2.0 / extreme=2.5 / more=1.5 / ish=0.75`，项目递进词表默认 `1.5`；一个词只归入一个情绪类别（`break` 提前跳出）。[^2]

### S3-4 词库扩展路径（古白话覆盖率提升）

| 资源 | 地址与说明 |
|---|---|
| ppzhenghua / SentimentAnalysisDictionary | <https://github.com/ppzhenghua/SentimentAnalysisDictionary> —— 8 类词典汇总（NTUSD／清华褒贬义／Hownet／大工本体／否定词／极值表／近义词），更新至 2025-03 |
| Shimon-Guo / chinese_sentiment_dictionary | <https://github.com/Shimon-Guo/chinese_sentiment_dictionary> —— 4 个主流词典精简包（Hownet／NTUSD／清华／BosonNLP） |
| DavionWu2018 / Sentiment_dictionary | <https://github.com/DavionWu2018/Sentiment_dictionary> —— 通用 + 金融情感词典 + 程度副词 + 否定词 + 停用词 + Python 示例代码 |
| 大连理工情感词汇本体（DUTIR） | <http://ir.dlut.edu.cn/info/1013/1142.htm> —— 七大类情绪本体，被引 437 次；项目 `基础/情感词典_DUTIR/` 即基于此 |
| 中文停用词表聚合（4 表并集） | <https://github.com/goto456/stopwords> —— 哈工大／百度／川大／中文停用词表，项目 `基础/停用词库/` 已收录 |
| 台湾大学 NTUSD | <http://nlg.csie.ntu.edu.tw/nlpresource/NTUSD-Fin/>（原站可能失效，建议走 GitHub 镜像） |
| 清华大学李军褒贬义词典 | <http://nlp.csai.tsinghua.edu.cn/site2/index.php/13-sms>（原站可能失效） |
| BosonNLP 情感词典 | <https://bosonnlp.com/dev/resource>（服务已停止，可用镜像） |

注意事项：

- 词典版权：仅供科研教学使用，商用需联系原作者（项目调研文档已明确标注）
- 编码差异：中文词典可能为 GBK，合并入 UTF-8 词库前需统一转码
- 合并策略：**新词只做并集、不覆盖既有词**，避免改变已发布报告的口径

### S3-5 验收标准

- [ ] `情感分析结果/` 下 **120** 个 `{章号}_emotion.json`，字段齐全
- [ ] 每章 `dutir_total > 0`；七类 count 之和 = `dutir_total`；pct 之和 ≈ 100%
- [ ] `dutir_pos + dutir_neg + dutir_neu == dutir_total`（极性映射一致性）
- [ ] 运行 `python check_dicts.py` 通过：DUTIR 去重后 27,389 词、Hownet pos+neg 17,690 词
- [ ] 抽查第 1 章：`hownet_pos_pct + hownet_neg_pct` 应接近 100%，偏差 > 2% 需排查

## S4 LLM 深度分析引擎（M4）· 工作量核心

### S4-1 目标与输入输出 `P0`

- **输入**：① 章节原文 ② `{章号}_emotion.json` ③ 提示词模板（V3.4 九大模块）④ 章节简称表 / 叙事阶段预分类表
- **输出**：`分析结果/{章号}_{简称}_V3.5_分析报告.md`（九大模块），并附带一份结构化 JSON（事件 / 人物 / 情绪 / 冲突 / 伏笔）

### S4-2 选型与理由

| 组件 | 选型 | 理由与地址 |
|---|---|---|
| API SDK | openai（OpenAI 兼容协议） | 一套代码可切换任意兼容后端（云端或本地）—— <https://github.com/openai/openai-python> |
| 本地模型（数据不出机） | Ollama / vLLM / LM Studio | <https://ollama.com/> · <https://github.com/vllm-project/vllm> · <https://lmstudio.ai/>（后两者暴露 OpenAI 兼容端点） |
| 模板引擎 | Jinja2 | 把「任务说明书 + 空白模板」参数化为模板 —— <https://jinja.palletsprojects.com/> |
| 结构化输出 | JSON Schema + Pydantic 校验 | 让 LLM 直接返回 JSON 并做 schema 校验 —— <https://docs.pydantic.dev/> |
| 任务编排 | asyncio + aiosqlite（自建 TaskQueue） | 断点续跑 / 增量跳过靠文件系统与 DB 双记录，无需引入 Celery 等重组件 |
| 长任务调度（可选） | APScheduler | 若需「夜间批量跑全书」—— <https://github.com/agronholm/apscheduler> |

### S4-3 安装命令

```bash
pip install openai jinja2 pydantic python-dotenv

# 本地模型（可选，二选一即可）
# 方式 A：Ollama
#   curl -fsSL https://ollama.com/install.sh | sh
#   ollama pull qwen2.5:14b
# 方式 B：vLLM 部署 OpenAI 兼容服务
#   pip install vllm && vllm serve Qwen/Qwen2.5-14B-Instruct
```

### S4-4 核心代码（模板化提示词 + 断点续跑 + JSON 校验）

```python
# analyze.py —— LLM 深度分析调度器
import os, glob, json, time
from jinja2 import Template
from openai import OpenAI
from config import ROOT, SPLIT_DIR, EMO_DIR, REPORT_DIR

client = OpenAI(
    api_key=os.getenv("LLM_API_KEY", "sk-local"),
    base_url=os.getenv("LLM_BASE_URL", "https://api.openai.com/v1"),
)
MODEL = os.getenv("LLM_MODEL", "gpt-4o-mini")

# ---------- 1. 提示词模板：由 V3.4 任务说明书 + 空白模板合成 ----------
PROMPT = Template("""你是《红楼梦》文本分析专家。请严格按「双轨六要素 V3.4」九大模块，
为第{{ no }}章（{{ title }}，{{ chars }} 字）生成分析报告。

【硬性规则】
1. 事件数下限 ≥ {{ chars // 500 }} 件；事件 ID 规则 E{{ no }}-{序号}；分级 ★/◆/◇。
2. §五 情感数据必须直接引用下方 JSON，不得自行估算。
3. §六 人物出场须标注段落索引。
4. 输出格式：先输出一个 json 代码块（见下方 SCHEMA），再输出完整 Markdown 报告。

【本章情绪数据 JSON】
{{ emotion_json }}

【结构化输出 SCHEMA】
{"chapter":int,"events":[{"id":str,"level":str,"summary":str,"para":int}],
 "persons":[{"name":str,"level":str,"para":int}],
 "emotion":{"tone":str,"dutir":{"好":int,"乐":int,"哀":int,"怒":int,"惧":int,"恶":int,"惊":int}},
 "foreshadows":[...],"conflicts":int}

【本章原文】
{{ text }}""")

# ---------- 2. 断点续跑：已生成且体积达标则跳过 ----------
def pending(range_=(1, 120)):
    todo = []
    for ch in range(range_[0], range_[1] + 1):
        hits = glob.glob(os.path.join(REPORT_DIR, f'{ch:03d}_*_V3.5_分析报告.md'))
        if hits and os.path.getsize(hits[0]) > 1000:
            continue                                    # 已完成 → 跳过
        if not os.path.exists(os.path.join(EMO_DIR, f'{ch:03d}_emotion.json')):
            print(f'⚠️ 第{ch}章: 情绪 JSON 不存在，跳过')
            continue
        todo.append(ch)
    return todo

# ---------- 3. 单章分析 ----------
def analyze_one(no):
    ch = f'{no:03d}'
    src = glob.glob(os.path.join(SPLIT_DIR, f'{ch}_*.txt'))[0]
    text = open(src, 'r', encoding='utf-8').read()
    title = os.path.basename(src).split('_', 2)[2].replace('.txt', '')
    emo = open(os.path.join(EMO_DIR, f'{ch}_emotion.json'), 'r', encoding='utf-8').read()

    prompt = PROMPT.render(no=no, title=title, chars=len(text),
                           emotion_json=emo, text=text)
    for attempt in range(3):                            # 失败重试 3 次
        try:
            resp = client.chat.completions.create(
                model=MODEL, temperature=0.2, max_tokens=8000,
                messages=[{"role": "user", "content": prompt}])
            md = resp.choices[0].message.content
            out = os.path.join(REPORT_DIR,
                               f'{ch}_{title.split("　")[0]}_V3.5_分析报告.md')
            open(out, 'w', encoding='utf-8').write(md)
            return True
        except Exception as e:
            print(f'  第{no}章 第{attempt+1}次失败: {e}')
            time.sleep(2 ** attempt)                    # 指数退避
    return False

if __name__ == '__main__':
    todo = pending((1, 120))
    print(f'待处理 {len(todo)} 章')
    for no in todo:
        ok = analyze_one(no)
        print(f'  第{no}章 {"✅" if ok else "❌"}')
```

### S4-5 质量门禁（生成后自动校验）

把现有 4 个验证脚本挂到分析流程之后，作为软件内的「质量门禁」服务：

```bash
# 每章生成后依次执行，任一失败即标记该章待重跑
python 脚本工具/验证检查类/verify_v32.py        # 大章节/子章节数、关键小节存在性
python 脚本工具/验证检查类/deep_verify.py       # 版本号一致性、占位符数量、完整性检测项
python 脚本工具/验证检查类/verify_alignment.py  # 全章节结构与最新模板对齐
python 脚本工具/验证检查类/check_dicts.py       # 情感词典完备性
```

- [ ] 事件数 ≥ 字数 ÷ 500（低于阈值自动重跑，并在日志中记录）
- [ ] 九大模块齐全，41 个三级标题无缺失
- [ ] §五 七类情绪数值与 `{章号}_emotion.json` 完全一致（不可 LLM 自行编造）
- [ ] 版本号三处一致（标题 / 头部信息块 / 页脚）

## S5 事件挖掘模块（M5）

### S5-1 目标与规则口径 `P1`

- **目标**：把 LLM 输出的事件条目规范化成可查询、可统计的事件表，并生成时间线。
- **已锁定规则**：① 事件 = 起因 → 行动 → 结果 的完整单元；② ID 规则 `E{章号}-{序号}`；③ 三级分级 ★（核心）/ ◆（重要）/ ◇（一般）；④ 必须标注原文段落索引；⑤ 事件数下限 = 字数 ÷ 500。

### S5-2 核心代码（结构化落盘 + 统计校验）

```python
# events.py —— 事件表构建与合规校验
import json, os
from collections import Counter
from config import REPORT_DIR

def parse_events(structured):
    """从 LLM 结构化输出中取出事件表"""
    rows = []
    for ev in structured.get('events', []):
        rows.append({
            'event_id': ev['id'],                 # E{章}-{序号}
            'chapter': structured['chapter'],
            'level': ev['level'],                 # ★ / ◆ / ◇
            'summary': ev['summary'],
            'para': ev.get('para'),               # 段落索引
            'five_w_one_h': ev.get('5w1h', {}),   # who/what/when/where/why/how
        })
    return rows

def check_events(rows, chars):
    """合规校验：数量下限 + 分级分布 + 段落索引完整"""
    low = chars // 500
    problems = []
    if len(rows) < low:
        problems.append(f'事件数 {len(rows)} < 下限 {low}')
    if any(r['para'] is None for r in rows):
        problems.append('存在缺少段落索引的事件')
    dist = Counter(r['level'] for r in rows)
    if not dist:
        problems.append('事件分级为空')
    return {'count': len(rows), 'lower_bound': low, 'level_dist': dict(dist),
            'problems': problems}
```

### S5-3 可视化：事件时间线

- ECharts（推荐）：<https://echarts.apache.org/zh/index.html> —— 时间线 / 关系图 / 曲线均由它统一承载
- vis-timeline：<https://github.com/visjs/vis-timeline> —— 专门做交互式时间轴，适合事件密度高的章节
- pyecharts：<https://github.com/pyecharts/pyecharts> —— 若想从 Python 侧直接产出 HTML 图表

验收标准：

- [ ] 每章事件数 ≥ 字数 ÷ 500，且 ★ 级事件占比在 10%–30% 之间（过高说明分级失效）
- [ ] 事件 ID 全书唯一、无重复、章内序号连续
- [ ] 时间线可按 ★/◆/◇ 分层筛选，点击事件可跳转到原文段落

## S6 人物挖掘模块（M6）

### S6-1 目标与输入输出 `P1`

- **输入**：① `基础/红楼梦人物名.txt`（362 行）② 各章报告的人物占位符 ③ 全文分词结果（nr 词性候选）
- **输出**：人物档案 JSON（首次出场章号 / 出场次数 / 层级 / 关系）+ 人物共现网络图 + 人物情感曲线

### S6-2 别名归并（ALIAS）—— 本模块的最大难点

同一人物在书中有多种称法（黛玉 / 林黛玉 / 林妹妹 / 颦儿），必须归一。项目已有 40+ 条 ALIAS 字典，需**配置化、可编辑、可导入导出**。

```python
# alias.py —— 别名归并字典（可外置为 alias.json 供界面编辑）
ALIAS = {
    # 别名 → 标准名
    "黛玉": "林黛玉", "林妹妹": "林黛玉", "颦儿": "林黛玉", "颦颦": "林黛玉",
    "凤姐": "王熙凤", "凤辣子": "王熙凤", "琏二奶奶": "王熙凤",
    "宝钗": "薛宝钗", "宝姐姐": "薛宝钗",
    "宝玉": "贾宝玉", "宝二爷": "贾宝玉", "怡红公子": "贾宝玉",
    "焙茗": "茗烟", "茗烟": "茗烟",
    "史太君": "贾母", "老太太": "贾母", "老祖宗": "贾母",
    "探春": "贾探春", "三姑娘": "贾探春", "蕉下客": "贾探春",
    # ... 40+ 条，建议抽成外部 JSON
}

def canon(name, alias=ALIAS):
    return alias.get(name.strip(), name.strip())
```

> **自动建议别名**
>
> 对「名字被包含」的候选对（如「宝玉」⊂「贾宝玉」、「黛玉」⊂「林黛玉」）自动生成待确认清单，人工勾选后写回 ALIAS —— 这比纯手工维护 40+ 条更可靠。项目 `analyze_persons_v2.py` 已实现别名检测的雏形，可直接扩展。

### S6-3 人名识别增强（可选）

- HanLP：<https://github.com/hankcs/HanLP> —— 中文 NER 效果好，内置人名 / 地名 / 机构名
- LTP：<https://github.com/HIT-SCIR/ltp> —— 哈工大语言技术平台，含 NER 与依存句法
- 百度 LAC：<https://github.com/baidu/lac> —— 词法分析，含人名标注
- bert-base-chinese：<https://huggingface.co/bert-base-chinese> —— 需要更高精度时的微调起点

> **先用词典、后上模型**
>
> 项目已有 362 行人物名表 + 663 行分词词典，词典法在《红楼梦》这类封闭人物集上已能覆盖绝大多数出场；NER 模型作为「发现漏网人物」的补充手段，不必作为主路径。

### S6-4 人物关系网络

```python
# persons.py —— 共现网络 + 关系图导出
import json, itertools
from collections import Counter
import networkx as nx

def build_graph(chapter_persons, alias=None):
    """chapter_persons: {章号: [人物, ...]}；同章共现即连边"""
    alias = alias or {}
    G = nx.Graph()
    for ch, names in chapter_persons.items():
        std = sorted({alias.get(n, n) for n in names})
        for a, b in itertools.combinations(std, 2):
            if G.has_edge(a, b):
                G[a][b]['weight'] += 1
                G[a][b]['chapters'].append(ch)
            else:
                G.add_edge(a, b, weight=1, chapters=[ch])
    return G

# 关键指标：度中心性（社交广度）、介数中心性（关系枢纽）
def key_metrics(G):
    return {
        'degree':  dict(nx.degree_centrality(G)),
        'between': dict(nx.betweenness_centrality(G)),
    }

# 导出交互式关系图（HTML）
from pyvis.network import Network
def export_html(G, path='人物关系图.html'):
    net = Network(height='720px', width='100%', bgcolor='#ffffff', font_color='#1f2d3d')
    for n in G.nodes:
        net.add_node(n, label=n, size=10 + 2 * G.degree(n))
    for a, b, d in G.edges(data=True):
        net.add_edge(a, b, value=d['weight'], title=f"共现 {d['weight']} 次")
    net.show(path, notebook=False)
```

- NetworkX：<https://networkx.org/>
- pyvis：<https://github.com/WestHealth/pyvis>（Python 侧快速出关系图）
- AntV G6：<https://github.com/antvis/G6>（前端侧关系图，交互更强）

验收标准：

- [ ] 人物档案去重后条目数与既有归档索引 §二 一致（宽口径约 260 人）
- [ ] 抽查 10 组别名，归并准确率 100%（黛玉 / 林妹妹 / 颦儿 → 林黛玉）
- [ ] 关系图节点度分布合理：贾宝玉 / 林黛玉 / 王熙凤 / 薛宝钗 应处于度中心性前列

## S7 报告与全局索引引擎（M7）

### S7-1 实现路径：结构化输出优先，正则提取兜底 `P0`

- **主路径**：LLM 直接返回 JSON（S4-4 的 SCHEMA 块）→ 入库 → 生成索引
- **兜底路径**：对历史 MD 报告沿用 `extract_v34_v2.py` 正则提取 → `gen_index.py` 生成索引（已有 17,320 行产物验证）

```bash
# 完整索引生成流程（两步走，与既有实践一致）
python 脚本工具/索引归档类/extract_v34_v2.py   # MD 报告 → v34_extract_v2.json
python 脚本工具/索引归档类/gen_index.py        # JSON → _归档索引_V3.4.md（7 大部分）
```

**两个核心配置表（务必外置为可编辑 JSON）**：

```python
# TONE_MAP：情感基调 14 种表述 → 6 个标准标签
TONE_MAP = {
    "悲凉": "悲", "凄婉": "悲", "哀伤": "悲", "沉郁": "悲",
    "欢愉": "喜", "喜庆": "喜", "轻松": "喜", "诙谐": "喜",
    "平和": "平", "恬淡": "平", "日常": "平",
    "紧张": "紧张", "激烈": "紧张", "肃杀": "紧张",
    "温馨": "温馨", "温情": "温馨",
    "荒诞": "荒诞", "诡谲": "荒诞",
}
```

### S7-2 验收标准

- [ ] `v34_extract_v2.json` 行数与历史一致（17,320 行量级），无解析异常章节
- [ ] `_归档索引_V3.4.md` 含完整 7 大部分，行数 1200+ 量级
- [ ] 抽查第 1/40/80/120 章的索引行，字数 / 事件数 / 主导情绪与报告正文一致
- [ ] TONE_MAP 归一后标准标签只出现 6 类，无「其他」残差
- [ ] 人物出场档案按首次出场章号升序排列（`fix_person_order.py` 校验通过）

## S8 可视化与导出（M8）

### S8-1 四类可视化的实现方案 `P1`

| 可视化 | 推荐库 | 数据来源与地址 |
|---|---|---|
| 章节情感曲线 | ECharts（折线 + 面积） | 逐章 `hownet_pos_pct / neg_pct` + 七类情绪堆叠；<https://echarts.apache.org/zh/index.html> |
| 事件时间线 | ECharts timeline / vis-timeline | 事件表（chapter + level + summary）；<https://github.com/visjs/vis-timeline> |
| 人物关系图谱 | AntV G6 / pyvis | 共现网络图；<https://github.com/antvis/G6> |
| 高频词云 | wordcloud | 各章 `top_words` 字段；<https://github.com/amueller/word_cloud> |

> **中文词云必读**
>
> wordcloud 默认字体不含中文，必须显式指定中文字体路径（否则全部显示为方框）。推荐思源黑体 —— <https://github.com/adobe-fonts/source-han-sans>。

```python
from wordcloud import WordCloud
wc = WordCloud(font_path='fonts/SourceHanSansSC-Regular.otf',   # ← 关键，勿省略
               width=1200, height=800, background_color='white',
               max_words=200).generate_from_frequencies(
                   {d['word']: d['count'] for d in top_words})
wc.to_file('词云_第001章.png')
```

### S8-2 导出方案

| 目标格式 | 工具 | 说明与地址 |
|---|---|---|
| Word (.docx) | python-docx | 从结构化 JSON 直接生成，格式最可控；<https://github.com/python-openxml/python-docx> |
| PDF | WeasyPrint / Pandoc + wkhtmltopdf | 本报告即「HTML → PDF」思路；<https://weasyprint.org/> · <https://pandoc.org/> · <https://wkhtmltopdf.org/> |
| Excel (.xlsx) | openpyxl | 索引导出（逐章统计 / 人物档案 / 情绪汇总 多 sheet）；<https://openpyxl.readthedocs.io/> |
| HTML | markdown-it / python-markdown | 报告阅读器渲染；<https://github.com/markdown-it/markdown-it> · <https://python-markdown.github.io/> |

验收标准：

- [ ] 导出的 PDF 页眉页脚含项目名与版本号；中文不出现方框或缺字
- [ ] Excel 导出的索引表中，数值列均为数字类型（非文本），可直接做数据透视
- [ ] Word 导出保留标题层级（Heading 1/2/3），可用导航窗格跳转

## 5.9 界面工作台设计（6 个）

| 工作台 | 主要功能 | 对应模块 / 步骤 |
|---|---|---|
| ① 导入与项目 | 导入 TXT → 自动拆分 → 项目配置（词库、模型、模板） | M1、M2 / S1 |
| ② 预处理 | 一键分词 + 情感统计，显示逐章进度与失败重试 | M2、M3 / S2、S3 |
| ③ 深度分析 | 任务队列（批量章节）、模板选择、LLM 参数、断点续跑 | M4 / S4 |
| ④ 事件视图 | 事件列表（E ID / 级别 / 段落索引 / 摘要）、5W1H 明细、事件时间线 | M5 / S5 |
| ⑤ 人物视图 | 人物卡片（首次出场 / 次数 / 关系）、人物关系图、人物情感曲线 | M6 / S6 |
| ⑥ 索引与导出 | 全局索引 7 部分预览、报告阅读器、导出（MD/HTML/Word/PDF/Excel） | M7、M8 / S7、S8 |

---

# 六、技术选型（附官方地址）

| 组件 | 选型 | 理由与官方地址 |
|---|---|---|
| 桌面形态 | 本地 Web App（FastAPI + 浏览器前端） | 数据留在本地、无需上传；可打包为桌面壳 —— Tauri <https://tauri.app/zh-cn/> / Electron <https://www.electronjs.org/zh/> |
| 后端 | Python 3.10 + FastAPI | 直接复用现有全部 Python 脚本与词库 —— <https://fastapi.tiangolo.com/zh/> · <https://www.uvicorn.org/> · <https://docs.pydantic.dev/> |
| 前端 | Vue3 + ECharts + Element Plus | ECharts 覆盖情感曲线 / 关系图 / 时间线全部可视化 —— <https://cn.vuejs.org/> · <https://cn.vitejs.dev/> · <https://echarts.apache.org/zh/index.html> · <https://element-plus.org/zh-CN/> |
| 存储 | SQLite + 文件系统 | 结构化索引与原始资产分离 —— <https://www.sqlite.org/> · <https://github.com/fastapi/sqlmodel> |
| 中文分词 | jieba（+ 用户词典） | 纯 Python、MIT、可加载 663 行专名词典 —— <https://github.com/fxsjy/jieba> |
| 情感分析 | 本地双引擎（DUTIR + Hownet） | 项目 `基础/emotion_analysis.py`，零依赖、结果可解释；词典可扩展 —— cnsenti <https://github.com/hiDaDeng/cnsenti> / cntext <https://github.com/hiDaDeng/cntext> |
| LLM 调度 | OpenAI 兼容 API / 本地模型 | 一套代码切换任意后端 —— <https://github.com/openai/openai-python> · <https://ollama.com/> · <https://github.com/vllm-project/vllm> |
| 任务编排 | 内置 TaskQueue（asyncio + aiosqlite） | 批处理、断点续跑、已生成跳过、失败重试与日志（S4-4 已实现雏形） |
| 校验门禁 | 移植 verify_v32 / deep_verify / verify_alignment / check_dicts | 报告生成后自动结构 / 数据完整性校验（S4-5） |
| 日志 | loguru | 逐章失败定位与重跑依据 —— <https://github.com/Delgan/loguru> |

---

# 七、实施路线图（里程碑 + 验收物）

1. **里程碑 1 · 引擎筑基**
   - **任务**：S0 环境与配置外置 → S1 章节拆分 → S2 分词 → S3 情感分析，跑通单章全流程；迁移 38 个脚本为模块；词库资产并入项目。
   - **验收物**：输入一份 TXT，产出 120 份拆分文件 + 120 份分词 JSON + 120 份情绪 JSON；`check_dicts.py` 通过。

2. **里程碑 2 · 自动化流水线**
   - **任务**：S4 LLM 深度分析调度（九大模块模板 + 任务队列 + 断点续跑 + 增量跳过）→ S7 结构化提取与索引生成 → S4-5 质量门禁接入。
   - **验收物**：一条命令跑完 120 章；中断后可续跑且不重复生成；生成后自动校验并列出不合格章节。

3. **里程碑 3 · 可视化与检索**
   - **任务**：S5 事件时间线 → S6 人物关系图谱与人物情感曲线 → S8 情感曲线 / 词云 / 报告阅读器 / Word·PDF·Excel 导出。
   - **验收物**：6 个工作台可用；图表可交互（点击事件跳原文段落）；导出文件中文无乱码。

4. **里程碑 4 · 通用化与开放**
   - **任务**：章节识别规则配置化（支持任意小说）→ 词库热更新（导入外部 5 库）→ 模板自定义 → LLM 后端可切换（含本地模型，数据不出本机）。
   - **验收物**：换一本非红楼小说，仅改配置即可跑通 S1–S3；词库可在界面内增删并即时生效。

5. **里程碑 5 · 打包与分发**（详见第九章）
   - **任务**：执行 jieba 瘦身与资产归拢（9.5.3）→ 前端构建（9.5.5）→ PyInstaller 后端 sidecar（9.5.2）→ Tauri 壳与 NSIS 安装包（9.5.6 / 9.5.7）；补齐 `THIRD_PARTY_NOTICES.md`。
   - **验收物**：**单个安装包（目标 ≤ 50 MB）**，在未装 Python / Node 的干净 Windows 10 或 11 上安装后可**离线**跑通 S1–S3，导出中文无乱码，退出无残留进程。

> **推进原则**
>
> 先打通「零依赖的 S1–S3」，再啃「最需要工程化封装的 S4」。S1–S3 全部可直接复用现有脚本，风险极低；S4 是唯一的中改造模块，也是整个软件的价值中枢。

---

# 八、风险与对策

| 风险 | 表现 | 对策（具体动作） |
|---|---|---|
| LLM 生成质量波动 | 事件 / 人物提取不全、格式漂移 | 九大模块模板约束 + S4-5 质量门禁自动执行四个 verify 脚本；不合格章节自动进入重跑队列；事件数低于「字数 ÷ 500」即判失败 |
| 情感词典覆盖不足 | 文言 / 古语词未被词典命中 | 按 S3-4 引入外部 5 库扩词；增加「未登录词巡检」（频次 ≥ 5 仍被切为单字的二字串）；新词只并集不覆盖，保持既有口径 |
| 结构化提取脆弱 | 报告格式漂移导致正则失败 | 主路径改为 LLM 直接输出 JSON + Pydantic schema 校验；`extract_v34_v2.py` 仅作历史报告兜底与交叉校验 |
| 人物别名 / 异体字 | 同一人物分裂为多条 | ALIAS 外置 JSON + 自动建议（包含关系候选）+ 人工确认；`analyze_persons_v2.py` 别名检测 |
| 分词口径被破坏 | 更换分词器导致 120 份历史产物失效 | 锁定 jieba 为唯一生产引擎；pkuseg / HanLP 仅作为补充分析工具，不替换既有产物 |
| jieba 性能与缓存 | 全量重跑慢；受限文件系统反复建缓存 | 进程内只 `initialize()` 一次；需要提速时切 jieba-fast（接口兼容）；显式设置 `jieba.dt.tmp_dir` |
| 中文导出乱码 | 词云方框、PDF 缺字 | 统一指定中文字体（思源黑体）；导出前做一次字符渲染自检 |
| 版本迁移 | 模板升级后旧报告失效 | 沿用归档规范与版本存档习惯；`v34_convert.py` 已验证可复用 |
| 数据一致性 | 索引与报告不同步 | extract → gen_index 两步串联 + 完整性检测日志；索引生成后抽查 4 个关键章（1/40/80/120） |
| 词典版权 | 商用风险 | 所有外部词典仅供科研教学；软件分发时附来源与许可说明，商用需联系原作者 |

---

# 九、打包与分发（单安装包体积预算 + 配置骨架）

> 本章回答一个具体问题：**这套软件做成「一个安装包」交给别人，到底多大？**
>
> 全部数字来自本机实测，不是估算 —— 工程资产逐文件扫描、jieba 装后体积实测、依赖 wheel 体积取自 PyPI 官方发布页。折算安装包时统一按 **压缩率 0.42**（纯 Python 源码压得动，`.pyd` / `.dll` 压不动，属保守取法）。

## 9.1 结论先行：四种形态的体积

| 形态 | 组成 | 安装后 | **安装包（约）** |
|---|---|---|---|
| **A 精简离线版** | S1–S3 预处理 + 网页界面 + Tauri 壳 | 88 MB | **37 MB** |
| **B 推荐全功能版** | A + LLM 调度 + 导出（Word/Excel）+ 关系图 | 132 MB | **55 MB** |
| C 精简版 + 离线词云 | A + numpy / pillow / wordcloud | 158 MB | 66 MB |
| D 推荐版 + Electron 壳 | B + Electron（内含 Chromium 全文） | 326 MB | 137 MB |

**附加项**

| 附加内容 | 安装后增量 | 安装包增量 | 说明 |
|---|---|---|---|
| 内置 120 章全量示例数据 | +15.7 MB | +6 MB | 属「可重算的中间产物」，建议做成按需下载而非内置 |
| 随包附一个本地小模型（数据不出本机） | +4～9 GB | — | 与上一行不在同一量级，按需另发 |

> **口径提醒**：上表是「**一个安装包把软件装完**」的口径。若允许首次启动联网拉依赖（pip / 模型），安装包可压到 10 MB 级，但会破坏「离线可用」这一核心卖点，不推荐。

## 9.2 实测依据（可复核）

### 9.2.1 工程资产：真正必须随包分发的只有约 1.3 MB

工程本体合计 **27.88 MB**，但其中大头是**可重算的中间产物**和示例数据：

| 目录 / 文件 | 体积 | 是否随包分发 |
|---|---|---|
| `分析结果/` | 10.05 MB | 示例数据，按需 |
| `skill/` 内两个 LDA 参考文件 | 3.71 MB | 否（开发资料） |
| `红楼梦_分词结果/` | 3.05 MB | 否（可重算） |
| `红楼梦_拆分/` | 2.45 MB | 否（可重算） |
| `红楼梦_分词结果_txt/` | 1.53 MB | 否（可重算） |
| 情感分析结果 / V3.3 存档包等 | 约 5.8 MB | 否 |
| **词库资产（`基础/` 词典与词表）** | **0.60 MB** | **是** |
| **脚本与提示词规范** | **0.70 MB**（脚本 0.25 + 规范 0.45） | **是** |
| **必须随包分发合计** | **≈ 1.3 MB** | — |

结论：**资产不是体积问题，运行时和依赖才是。**

### 9.2.2 jieba：单项冠军，装后 40.97 MB

本机虚拟环境实测（非 wheel 体积，是解包安装后的占用）：

| 组成 | 体积 | 本工程是否用得到 |
|---|---|---|
| `posseg` 词性标注概率表 | 约 12.0 MB | 用得到（词性过滤） |
| `lac_small/` 词向量模型 | 10.23 MB | **用不到** |
| `analyse/idf.txt`（TF-IDF 语料） | 5.91 MB | **用不到**（本工程走词频统计） |
| `dict.txt` 主词典 | 4.84 MB | 用得到 |
| `__pycache__/` | 4.20 MB | 运行时自动重建 |
| `finalseg/` + 其余代码 | 约 3.8 MB | 用得到（HMM 未登录词） |
| **合计** | **40.97 MB** | — |

### 9.2.3 运行时与第三方依赖

| 组件 | 实测体积 | 来源 |
|---|---|---|
| 托管 Python 3.13.12 整体（含 pip / tk / 测试套件） | 48.34 MB | 本机实测；裁剪后约 20 MB |
| jieba 发布包 | 19.2 MB / 仅源码包，**无 wheel** | PyPI 发布页 |
| numpy wheel | 12.6 MB | PyPI 发布页 |
| pillow wheel | 7.2 MB | PyPI 发布页 |
| lxml wheel | 4.0 MB | PyPI 发布页 |
| networkx wheel | 2.1 MB | PyPI 发布页 |
| pydantic-core wheel | 2.1 MB | PyPI 发布页 |
| 前端静态产物（Vue + ECharts + Element Plus，gzip 前） | 约 3.0 MB | 构建估算 |
| Tauri 壳（Rust 编译产物 + 资源） | 约 6.0 MB | 取决于是否静态链接 WebView2 绑定 |

## 9.3 三个省体积的杠杆（按收益排序）

| 措施 | 省安装后 | 省安装包 | 代价 |
|---|---|---|---|
| ① **用 ECharts 词云扩展替掉 Python `wordcloud`** | −70.5 MB | −30 MB | 需前端接 `echarts-wordcloud`；词云样式略受限于前端渲染 |
| ② **jieba 瘦身**（删 `lac_small` / `analyse` / `__pycache__`） | −20.3 MB | −9 MB | 不能再调 `jieba.analyse.extract_tags` / `textrank` |
| ③ **壳用 Tauri 而非 Electron** | −194 MB | −80 MB | 目标机需有 WebView2（Windows 10/11 基本自带，缺失时安装器按需下载） |

> **① 最划算**：为了画一个词云，把 numpy + pillow + wordcloud 整套拖进来，而前端**已经有 ECharts**。这是典型的「重复造轮子还更重」。
>
> **③ 的取舍**：Electron 那 194 MB 换来的只是省掉 WebView2 的兼容顾虑。除非要支持很老的系统，否则不值。

## 9.4 推荐落点

| 方案 | 安装后 | 安装包 |
|---|---|---|
| B 版基线（Tauri 壳） | 132 MB | 约 55 MB |
| B 版 + 杠杆 ②（jieba 瘦身） | 111 MB | 约 47 MB |
| **B 版 + 杠杆 ①②（推荐）** | **111 MB** | **约 45～47 MB** |
| 再加全量示例数据 | 127 MB | 约 51 MB |

**目标值：安装包 ≤ 50 MB。** 这是投入产出最好的落点 —— 相比 Electron 方案省下的 80 MB，比后面所有优化加起来还多。

## 9.5 打包配置骨架

### 9.5.1 总体形态

```text
┌─────────────────────────────────────────────────────────┐
│ Tauri 壳（Rust，约 6 MB）                                │
│  ├── WebView2 加载 frontend/dist（Vue3 + ECharts）        │
│  └── sidecar 子进程 ← 启动/守护/退出时清理                 │
│        └── honloumeng-server.exe（PyInstaller onedir）    │
│              ├── app/           业务代码                   │
│              ├── assets/dicts/  DUTIR / Hownet / 停用词…   │
│              ├── assets/prompts/ 九大模块提示词规范         │
│              ├── assets/web/    前端构建产物（可选内置）     │
│              └── jieba/         dict.txt + finalseg + posseg│
└─────────────────────────────────────────────────────────┘
```

选择 **onedir 而非 onefile**：onefile 每次启动都要把几十 MB 解压到临时目录，首次启动会明显卡顿；onedir 目录结构清晰、便于打补丁。

### 9.5.2 S-P1 后端：PyInstaller spec

```python
# backend.spec —— Python 后端 + 词库资产单目录打包
# 用法：pyinstaller backend.spec --noconfirm --distpath dist/backend
import sys
from pathlib import Path
from PyInstaller.utils.hooks import collect_data_files

ROOT = Path(SPECPATH).parent
ASSETS = ROOT / "assets"          # 打包前把 基础/ 词库与规范拷到这里

datas = [
    (str(ASSETS / "dicts"),   "assets/dicts"),    # DUTIR / Hownet / 停用词 / 否定词 / 递进词
    (str(ASSETS / "prompts"), "assets/prompts"),  # 九大模块 LLM 提示词规范
    (str(ASSETS / "web"),     "assets/web"),      # Vite 构建产物 dist/
]
# jieba 的词典与模型必须以数据文件形式带上；已瘦身的目录直接排除
datas += collect_data_files("jieba", exclude=["**/lac_small/**", "**/analyse/**", "**/__pycache__/**"])

excludes = [
    "tkinter", "test", "unittest", "pydoc_data", "lib2to3",
    "matplotlib", "scipy", "pandas", "IPython", "notebook",
]

a = Analysis(
    ["app/main.py"],
    pathex=[str(ROOT)],
    binaries=[],
    datas=datas,
    hiddenimports=[
        "uvicorn.logging", "uvicorn.loops.auto",
        "uvicorn.protocols.http.auto", "uvicorn.protocols.websockets.auto",
        "uvicorn.lifespan.on", "aiosqlite", "jieba.posseg", "jieba.finalseg",
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=["scripts/pyi_rth_jieba.py"],   # 见 9.5.4
    excludes=excludes,
    noarchive=False,
)

pyz = PYZ(a.pure)

exe = EXE(
    pyz, a.scripts, [],
    exclude_binaries=True,
    name="honloumeng-server",
    console=False,                                # 生产形态无黑框；排障时改 True
    icon=str(ASSETS / "icon.ico"),
)

coll = COLLECT(
    exe, a.binaries, a.datas,
    strip=False,
    upx=False,                                    # 不建议 UPX：会拖慢启动且易被杀软误报
    name="honloumeng-server",
)
```

### 9.5.3 S-P2 jieba 瘦身脚本

```python
# scripts/slim_jieba.py —— 删除 jieba 中本工程用不到的资源，装后约省 20.3 MB
# 必须在 pyinstaller 之前运行
import shutil
import site
from pathlib import Path

DROP = [
    "lac_small",    # 词向量模型        10.23 MB —— 本工程不调用
    "analyse",      # TF-IDF / TextRank   5.91 MB —— 本工程走词频统计
    "__pycache__",  #                    4.20 MB —— 运行时重建
]

pkg = Path(site.getsitepackages()[0]) / "jieba"
if not pkg.exists():
    raise SystemExit(f"未找到 jieba：{pkg}")

freed = 0.0
for name in DROP:
    p = pkg / name
    if p.is_dir():
        freed += sum(f.stat().st_size for f in p.rglob("*") if f.is_file())
        shutil.rmtree(p, ignore_errors=True)
    elif p.exists():
        freed += p.stat().st_size
        p.unlink()

print(f"jieba 瘦身完成，释放 {freed / 1048576:.2f} MB：{pkg}")
```

> **注意**：瘦身之后**不可**再调用 `jieba.analyse.extract_tags()` / `textrank()`。若将来需要 TF-IDF 关键词，请改为在 `assets/dicts/` 自带一份精简 idf 语料，而不是恢复 `analyse/`。

### 9.5.4 S-P3 运行时资源定位与 jieba 初始化

```python
# scripts/pyi_rth_jieba.py —— PyInstaller runtime hook
# 作用：冻结环境下显式指定 jieba 缓存目录，避免受限文件系统写入失败
import os
import tempfile

_cache = os.path.join(tempfile.gettempdir(), "hlm-jieba")
os.makedirs(_cache, exist_ok=True)
os.environ.setdefault("JIEBA_CACHE_DIR", _cache)
```

```python
# app/bootstrap.py —— 资源定位 + jieba 初始化（全进程只调一次）
import os
import sys
import tempfile
from pathlib import Path

import jieba

def resource_path(*parts) -> Path:
    """冻结态从 _MEIPASS 取，开发态从工程根取。"""
    base = Path(getattr(sys, "_MEIPASS", Path(__file__).resolve().parents[1]))
    return base.joinpath(*parts)

def init_jieba() -> None:
    jieba.dt.tmp_dir = os.path.join(tempfile.gettempdir(), "hlm-jieba")
    os.makedirs(jieba.dt.tmp_dir, exist_ok=True)
    jieba.initialize()                      # 必须在循环外只调一次
    jieba.load_userdict(str(resource_path("assets", "dicts", "红楼梦分词词典.txt")))

def web_root() -> Path:
    """前端静态目录：优先内置产物，其次开发态 frontend/dist。"""
    bundled = resource_path("assets", "web")
    return bundled if bundled.exists() else Path(__file__).resolve().parents[1] / "frontend" / "dist"
```

### 9.5.5 S-P4 前端构建

```bash
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm i echarts echarts-wordcloud element-plus axios pinia vue-router
npm i -D unplugin-auto-import unplugin-vue-components
npm run build        # 产物 frontend/dist
```

```ts
// frontend/vite.config.ts —— 两条关键配置
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  base: './',                       // 关键：WebView2 以 file:// 载入时资源路径必须相对
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    chunkSizeWarningLimit: 1500,    // ECharts 单块较大，仅提示不告警
  },
  server: {
    proxy: { '/api': 'http://127.0.0.1:8710' },   // 开发期直连 sidecar
  },
})
```

> **词云方案（对应杠杆 ①）**：用 `echarts-wordcloud` 在前端画词云，**不要**引入 Python `wordcloud` —— 后者会拖进 numpy + pillow 共 70.5 MB。
> 地址：<https://github.com/ecomfe/echarts-wordcloud>

### 9.5.6 S-P5 桌面壳：Tauri 配置

```json
// src-tauri/tauri.conf.json —— Tauri v2
{
  "productName": "红楼梦文本分析",
  "version": "2.0.0",
  "identifier": "cn.example.hongloumeng.analysis",
  "build": {
    "frontendDist": "../frontend/dist",
    "beforeBuildCommand": "npm run build",
    "beforeDevCommand": "npm run dev"
  },
  "bundle": {
    "active": true,
    "targets": ["nsis"],
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/icon.ico"],
    "resources": ["assets/samples/**/*"],
    "externalBin": ["binaries/honloumeng-server"],
    "windows": {
      "webviewInstallMode": { "type": "downloadBootstrapper" }
    },
    "nsis": {
      "languages": ["SimpChinese"],
      "displayLanguageSelector": false,
      "installMode": "currentUser",
      "compression": "lzma"
    }
  }
}
```

> **命名约定（最容易踩的坑）**：`externalBin` 里只写不带后缀的名字，实际文件**必须**带目标三元组后缀，否则构建直接报错：
> `src-tauri/binaries/honloumeng-server-x86_64-pc-windows-msvc.exe`
>
> `webviewInstallMode` 用 `downloadBootstrapper`（按需下载，约 1.5 MB 引导器），**不要**用 `embedBootstrapper` 或 `offlineInstaller` —— 后者会把 100+ MB 的 WebView2 运行时装进你的安装包，前功尽弃。

```rust
// src-tauri/src/main.rs —— 启动 / 守护 / 退出清理 sidecar
use std::sync::Mutex;
use tauri::{Manager, RunEvent};
use tauri_plugin_shell::process::CommandChild;
use tauri_plugin_shell::ShellExt;

struct Sidecar(Mutex<Option<CommandChild>>);

fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_shell::init())
        .manage(Sidecar(Mutex::new(None)))
        .setup(|app| {
            let handle = app.handle().clone();
            let cmd = handle
                .shell()
                .sidecar("honloumeng-server")?
                .args(["--host", "127.0.0.1", "--port", "8710"]);
            let (_rx, child) = cmd.spawn()?;                 // 端口被占用时由后端自行换端口并写回
            app.state::<Sidecar>().0.lock().unwrap().replace(child);
            Ok(())
        })
        .build(tauri::generate_context!())
        .expect("Tauri 初始化失败")
        .run(|app, event| {
            // 主窗口退出时必须杀掉后端，避免残留进程占端口
            if let RunEvent::Exit = event {
                if let Some(child) = app.state::<Sidecar>().0.lock().unwrap().take() {
                    let _ = child.kill();
                }
            }
        });
}
```

### 9.5.7 S-P6 安装包：Tauri NSIS（首选）/ Inno Setup（备选）

**首选**：Tauri 内置 NSIS 目标，`cargo tauri build` 后产物在 `src-tauri/target/release/bundle/nsis/*.exe`，无需额外脚本，体积已由 `compression: "lzma"` 压过。

**备选**：需要更细的安装逻辑（custom 页面、附加 VC 运行库检测、卸载时询问是否保留项目数据）时，改用 Inno Setup 6：

```ini
; installer.iss —— 备选方案：Inno Setup 6
#define AppName "红楼梦文本分析"
#define AppVersion "2.0.0"
#define AppExe "红楼梦文本分析.exe"

[Setup]
AppId={{8F3C2B41-7E5A-4D6E-9C11-2A7B5D0E4F31}
AppName={#AppName}
AppVersion={#AppVersion}
DefaultDirName={autopf}\{#AppName}
DefaultGroupName={#AppName}
OutputBaseFilename=HLM-Analysis-Setup-{#AppVersion}-x64
Compression=lzma2/max
SolidCompression=yes
LZMAUseSeparateProcess=yes
ArchitecturesAllowed=x64compatible
ArchitecturesInstallIn64BitMode=x64compatible
WizardStyle=modern

[Files]
Source: "dist\红楼梦文本分析\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

[Tasks]
Name: "desktopicon"; Description: "创建桌面快捷方式"; Flags: unchecked

[Icons]
Name: "{group}\{#AppName}"; Filename: "{app}\{#AppExe}"
Name: "{autodesktop}\{#AppName}"; Filename: "{app}\{#AppExe}"; Tasks: desktopicon

[Run]
Filename: "{app}\{#AppExe}"; Description: "启动 {#AppName}"; Flags: nowait postinstall skipifsilent
```

> Inno Setup 下载：<https://jrsoftware.org/isinfo.php>　NSIS 文档：<https://nsis.sourceforge.io/Docs/>

### 9.5.8 S-P7 一键构建脚本

```powershell
# build.ps1 —— 瘦身 jieba → 前端 → 后端 sidecar → Tauri 安装包
$ErrorActionPreference = "Stop"
$triple = "x86_64-pc-windows-msvc"

Write-Host "[1/5] 准备资产与 jieba 瘦身"
python scripts/prepare_assets.py
python scripts/slim_jieba.py

Write-Host "[2/5] 构建前端"
Push-Location frontend; npm ci; npm run build; Pop-Location

Write-Host "[3/5] 打包后端 sidecar"
pyinstaller backend.spec --noconfirm --distpath dist/backend

Write-Host "[4/5] 同步 sidecar 到 Tauri（必须带目标三元组后缀）"
Copy-Item "dist/backend/honloumeng-server.exe" `
          "src-tauri/binaries/honloumeng-server-$triple.exe" -Force

Write-Host "[5/5] 构建安装包"
Push-Location src-tauri; cargo tauri build; Pop-Location

Write-Host "产物：src-tauri/target/release/bundle/nsis/"
Get-ChildItem "src-tauri/target/release/bundle/nsis/*.exe" |
    ForEach-Object { "{0}  {1:N1} MB" -f $_.Name, ($_.Length / 1MB) }
```

## 9.6 分发前自检清单（验收标准）

| # | 检查项 | 方法 | 通过标准 |
|---|---|---|---|
| 1 | 干净机安装 | 未装 Python / Node 的 Windows 10 或 11 虚拟机装包后直接启动 | 主界面可打开，无「缺少 DLL / api-ms-win」报错 |
| 2 | 离线可用 | 断网后跑完整 S1–S3 | 产出 120 份拆分 + 120 份分词 JSON + 120 份情绪 JSON |
| 3 | 中文不乱码 | 导出 Word / PDF / 词云各一份 | 无方框、无乱码，词云字体正常 |
| 4 | 体积复核 | 右键属性查看安装后目录 | **≤ 目标值**（推荐版 ≤ 120 MB） |
| 5 | 端口冲突 | 先占用 8710 再启动软件 | 后端自动换端口，前端仍能连上（或给出明确提示） |
| 6 | 进程清理 | 关闭主窗口后查任务管理器 | 无残留 `honloumeng-server.exe` |
| 7 | 卸载干净 | 控制面板卸载后检查 `%APPDATA%` / `%LOCALAPPDATA%` | 程序目录清空；用户项目数据按用户选择保留或清除 |
| 8 | 词库口径 | 用同一份 TXT 重跑，对比 V3.4 归档产物 | 词频与情感数值一致（词库只并集、不覆盖） |
| 9 | 许可合规 | 检查随包 `THIRD_PARTY_NOTICES.md` | 覆盖 jieba / 外部词典 / 字体 / WebView2，见 9.7 |

## 9.7 许可与合规

| 组件 | 许可 | 分发注意 |
|---|---|---|
| jieba | MIT | 保留版权声明即可随包分发 |
| Python 运行时 | PSF License | 随包保留 `LICENSE.txt` |
| PyInstaller | GPL-2.0 **with bootloader exception** | 打包产物不受 GPL 传染，可闭源分发 |
| Tauri | MIT / Apache-2.0 | 保留声明 |
| ECharts / echarts-wordcloud | Apache-2.0 | 保留 NOTICE |
| 思源黑体 | SIL OFL 1.1 | 可与软件一同分发，需保留 OFL 文本 |
| WebView2 Runtime | 微软许可 | **用 `downloadBootstrapper` 按需下载**，不要直接内嵌再分发 |
| DUTIR / Hownet / NTUSD / 清华李军词典 | 多为「科研教学免费」 | **软件若商用须联系原作者授权**；随包附来源与许可说明 |

> **原项目已定原则的延伸**：词库扩展**只做并集、不覆盖**（见第八章）。这一条在分发场景同样成立 —— 用户自行导入的词典不得改动既有 120 章产物的数值口径，否则同一份文本在不同机器上会得出不同结论。

建议随包放一份 `THIRD_PARTY_NOTICES.md`，逐条列出上表的组件、版本、许可与原文链接。

## 9.8 持续集成骨架（可选）

```yaml
# .github/workflows/build.yml
name: build-windows
on: [workflow_dispatch]
jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - uses: actions/setup-node@v4
        with: { node-version: "22" }
      - uses: dtolnay/rust-toolchain@stable
      - run: pip install -r requirements.txt pyinstaller
      - run: python scripts/prepare_assets.py
      - run: python scripts/slim_jieba.py
      - run: cd frontend && npm ci && npm run build
      - run: pyinstaller backend.spec --noconfirm --distpath dist/backend
      - run: |
          copy dist\backend\honloumeng-server.exe `
               src-tauri\binaries\honloumeng-server-x86_64-pc-windows-msvc.exe
      - run: cd src-tauri && cargo tauri build
      - uses: actions/upload-artifact@v4
        with:
          name: installer
          path: src-tauri/target/release/bundle/nsis/*.exe
```

## 9.9 本章与其他章节的对应

| 本章小节 | 对应前文 | 说明 |
|---|---|---|
| 9.5.2 / 9.5.4 | 第五章 S0 环境准备 | S0 是开发态选型，本节是冻结态的路径与资源定位 |
| 9.5.3 | 第八章「jieba 性能与缓存」风险 | 同一措施的双重收益：既提速也省 20.3 MB |
| 9.5.5 | 第五章 S8 可视化 | 词云从前端出，直接替掉 Python `wordcloud` 依赖 |
| 9.6 第 8 项 | 第八章「数据一致性」「分词口径」 | 分发不改变口径，是既有原则的延伸 |
| 整章 | 第七章 里程碑 5 · 打包与分发 | 本章是该里程碑的施工图 |

---

# 附录 A 外部资源地址总表

## A.1 中文分词与文本处理

| 名称 | 地址 |
|---|---|
| jieba（首选） | <https://github.com/fxsjy/jieba>　PyPI：<https://pypi.org/project/jieba/> |
| jieba 大词典（繁体） | <https://github.com/fxsjy/jieba/raw/master/extra_dict/dict.txt.big> |
| jieba 小词典 | <https://github.com/fxsjy/jieba/raw/master/extra_dict/dict.txt.small> |
| jieba-fast（C 加速） | <https://pypi.org/project/jieba-fast/>　源码：<https://github.com/deepcs233/jieba_fast> |
| cppjieba / gojieba / nodejieba | <https://github.com/yanyiwu/cppjieba>　<https://github.com/yanyiwu/gojieba>　<https://github.com/yanyiwu/nodejieba> |
| 结巴分词 Java 版 | <https://github.com/huaban/jieba-analysis> |
| pkuseg（领域分词） | <https://github.com/lancopku/pkuseg-python> |
| HanLP | <https://github.com/hankcs/HanLP> |
| LTP（哈工大） | <https://github.com/HIT-SCIR/ltp> |
| THULAC（清华） | <https://github.com/thunlp/THULAC-Python> |
| SnowNLP | <https://github.com/isnowfy/snownlp> |
| 百度 LAC | <https://github.com/baidu/lac> |
| cn2an（中文数字转换） | <https://github.com/Ailln/cn2an>　PyPI：<https://pypi.org/project/cn2an/> |
| chardet（编码探测） | <https://github.com/chardet/chardet> |
| ebooklib（EPUB 解析） | <https://github.com/aerkalov/ebooklib> |

## A.2 情感词典与情感分析

| 名称 | 地址 |
|---|---|
| cnsenti | <https://github.com/hiDaDeng/cnsenti>　PyPI：<https://pypi.org/project/cnsenti/> |
| cntext（cnsenti 升级版） | <https://github.com/hiDaDeng/cntext> |
| DUTIR 情感词汇本体 | <http://ir.dlut.edu.cn/info/1013/1142.htm>（大连理工，七大类情绪） |
| ppzhenghua（8 类词典汇总） | <https://github.com/ppzhenghua/SentimentAnalysisDictionary> |
| Shimon-Guo（4 类精简） | <https://github.com/Shimon-Guo/chinese_sentiment_dictionary> |
| DavionWu2018（含代码） | <https://github.com/DavionWu2018/Sentiment_dictionary> |
| 知网 Hownet 词语集 | <http://www.keenage.com/html/c_bulletin_2007.htm>（原站可能失效） |
| 台湾大学 NTUSD | <http://nlg.csie.ntu.edu.tw/nlpresource/NTUSD-Fin/>（原站可能失效） |
| 清华大学李军褒贬义词典 | <http://nlp.csai.tsinghua.edu.cn/site2/index.php/13-sms>（原站可能失效） |
| BosonNLP 情感词典 | <https://bosonnlp.com/dev/resource>（服务已停） |
| 中文停用词表聚合 | <https://github.com/goto456/stopwords> |

## A.3 后端、前端与可视化

| 名称 | 地址 |
|---|---|
| FastAPI | <https://fastapi.tiangolo.com/zh/>　源码：<https://github.com/fastapi/fastapi> |
| Uvicorn | <https://www.uvicorn.org/> |
| Pydantic | <https://docs.pydantic.dev/> |
| SQLModel / SQLAlchemy | <https://github.com/fastapi/sqlmodel>　<https://www.sqlalchemy.org/> |
| SQLite | <https://www.sqlite.org/> |
| Jinja2（模板引擎） | <https://jinja.palletsprojects.com/> |
| OpenAI Python SDK | <https://github.com/openai/openai-python> |
| Ollama（本地模型） | <https://ollama.com/> |
| vLLM（本地推理服务） | <https://github.com/vllm-project/vllm> |
| LM Studio | <https://lmstudio.ai/> |
| Vue 3 / Vite | <https://cn.vuejs.org/>　<https://cn.vitejs.dev/> |
| ECharts | <https://echarts.apache.org/zh/index.html> |
| Element Plus | <https://element-plus.org/zh-CN/> |
| AntV G6（关系图） | <https://github.com/antvis/G6> |
| NetworkX / pyvis | <https://networkx.org/>　<https://github.com/WestHealth/pyvis> |
| vis-timeline | <https://github.com/visjs/vis-timeline> |
| pyecharts | <https://github.com/pyecharts/pyecharts> |
| Tauri / Electron | <https://tauri.app/zh-cn/>　<https://www.electronjs.org/zh/> |

## A.4 导出与文档生成

| 名称 | 地址 |
|---|---|
| python-docx（Word） | <https://github.com/python-openxml/python-docx> |
| openpyxl（Excel） | <https://openpyxl.readthedocs.io/> |
| WeasyPrint（HTML→PDF） | <https://weasyprint.org/> |
| Pandoc（万能转换） | <https://pandoc.org/> |
| wkhtmltopdf | <https://wkhtmltopdf.org/> |
| python-markdown | <https://python-markdown.github.io/> |
| markdown-it（前端渲染） | <https://github.com/markdown-it/markdown-it> |
| wordcloud（词云） | <https://github.com/amueller/word_cloud> |
| 思源黑体（中文字体） | <https://github.com/adobe-fonts/source-han-sans> |
| loguru（日志） | <https://github.com/Delgan/loguru> |
| APScheduler（定时批量） | <https://github.com/agronholm/apscheduler> |

---

# 附录 B 依赖清单与一键安装

## B.1 生产依赖（软件运行必需）

```text
# requirements.txt
# —— 层 2 预处理（必需，极轻）——
jieba>=0.42.1

# —— 层 2 辅助 ——
cn2an>=0.5.22
chardet>=5.2.0

# —— 层 3 LLM 调度 ——
openai>=1.30.0
jinja2>=3.1.4
pydantic>=2.7.0
python-dotenv>=1.0.1
loguru>=0.7.2

# —— 服务层 ——
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlmodel>=0.0.19
aiosqlite>=0.20.0

# —— 层 4 可视化与导出 ——
networkx>=3.3
pyvis>=0.3.2
wordcloud>=1.9.3
pyecharts>=2.0.5
openpyxl>=3.1.2
python-docx>=1.1.0
```

```bash
# 一键安装
pip install -r requirements.txt

# 可选：分词加速（仅在全量重跑耗时明显时启用）
pip install jieba-fast

# 可选：本地模型（数据不出本机）
# pip install vllm && vllm serve Qwen/Qwen2.5-14B-Instruct
```

```bash
# 仅「构建机」需要的打包依赖（用户机器不需要，见第九章）
pip install pyinstaller
cargo install tauri-cli        # 或 npm i -g @tauri-apps/cli
```

## B.2 项目自带资源（随包分发，无需安装）

```text
基础/
├── emotion_analysis.py      # 双引擎：EmotionAnalyzer + SentimentAnalyzer（零依赖）
├── 情感词典_DUTIR/          # 七类情绪词典（27,414 词标称 / 27,389 词去重）
├── 情感词典_Hownet/         # 极性词典 + 程度副词 + deny（19,472 词标称 / 17,690 去重）
├── 停用词.txt + 停用词库/    # 4 表并集（3,884 词）
├── 否定词.txt               # 1,517 词（奇数次翻转、偶数次还原）
├── 递进词.txt               # 28 词（默认权重 1.5）
├── 红楼梦人物名.txt          # 362 行
├── 红楼梦分词词典.txt        # 663 行（jieba 用户词典）
├── 红楼梦章节名.txt
└── 同义词.txt / 反义词.txt   # 词林扩展版 / 反义词对表（非情感词典）
```

## B.3 环境变量模板

```text
# .env
LLM_BASE_URL=https://api.openai.com/v1     # 或 http://localhost:11434/v1（Ollama）
LLM_API_KEY=sk-xxxxxxxx
LLM_MODEL=gpt-4o-mini                       # 或 qwen2.5:14b（本地）
PROJECT_ROOT=./红楼梦范例
LOG_LEVEL=INFO
```

---

# 主要数据来源

1. [^1] `红楼梦范例/分析结果/_归档索引_V3.4.md`（1207 行，7 大部分全局索引）
2. [^2] `红楼梦范例/基础/emotion_analysis.py`（EmotionAnalyzer + SentimentAnalyzer 双引擎）
3. [^3] `红楼梦范例/分析结果/v34_extract_v2.json`（17,320 行结构化提取数据）
4. [^4] `红楼梦范例/脚本工具/索引归档类/gen_index.py`（ALIAS 别名字典 + TONE_MAP 基调归一）
5. `红楼梦范例/脚本工具/README.md`（38 个脚本的功能与用法说明，V3.4.1）
6. `红楼梦范例/skill/红楼梦专用版_前置条件与生成规范.md` §13.8 完整数据管线一键脚本
7. `红楼梦范例/情感词库资源统合/` 8 份调研文档（含 5 个公开情感词典资源地址清单）
8. `红楼梦范例/红楼梦文本分析软件设计方案.html`（V1.0 架构版）

[^1]: 全局归档索引，含归档索引表、人物出场档案、逐章累计统计、伏笔线索追踪、七类情绪分析汇总、情感基调演变趋势、全局统计摘要。
[^2]: 双引擎实现细节：否定词窗口 = 5，奇数次翻转、偶数次还原；程度权重 very=2.0 / extreme=2.5 / more=1.5 / ish=0.75。
[^3]: 由 `脚本工具/索引归档类/extract_v34_v2.py` 从 80 份 MD 报告正则提取生成。
[^4]: ALIAS 人物别名归并字典（40+ 条）+ TONE_MAP 情感基调归一（14 种表述 → 6 标准标签）。

---

> **机制已齐，步骤已明，工程化即软件**
>
> V1.0 回答了「能做成什么」；V2.0 回答了「具体怎么做」—— 8 个模块、23 个施工步骤，每一步都有选型理由、安装命令、可运行代码、验收标准与官方地址；第九章再把成果收进「一个安装包」，实测推荐形态约 55 MB、优化后约 45 MB。从零依赖的 S1–S3 起步，5 个里程碑即可产出**可分发的**桌面版软件。

---

*文档版本：V2.0 · 2026-09-16 · Markdown 版（含第九章 打包与分发）*
