# 2026-09-17 工作日志

## 定时任务总览：增补 06 号（WorkBuddy 积分签到）

用户要求：**"增加一点10分的任务计划程序，WorkBuddy的脚本到文件夹中更新md文件。"**

### 事实核对（先说结论）
- **`WorkBuddy积分签到` 任务已存在**（不是新建）：触发 `2026-09-17T01:10:00+08:00` 每天 01:10，
  动作 `cmd.exe /s /c ""…\run_workbuddy_checkin.cmd" >> "…\checkin_workbuddy.log" 2>&1"`，
  上次运行 2026-09-17 04:32:30（注册后的手动验证）结果 0，下次 2026-09-18 01:10。
  → 所以本次实际要做的是「把脚本纳入总览文件夹 + 更新 md」，任务本身无需再注册。
- 脚本原件在 `C:\Users\Administrator\Desktop\定时任务脚本\`：`run_workbuddy_checkin.cmd` + `workbuddy_checkin.js`。

### 交付物
1. 新建 `定时任务总览\06_WorkBuddy积分签到（每天01点10分）\`
   - `run_workbuddy_checkin.cmd`（458 B）+ `workbuddy_checkin.js`（4782 B）副本
   - `README.md`（9.9 KB，十节结构，与 01–05 体例一致）
2. 更新 `定时任务总览\README.md`（9.6 KB）：5 → **6** 个任务；补 06 行、目录树、副本对照表、
   `02/03/04/05/06` 改时间说明；新增「任务间的隐藏依赖」小节；维护约定里的示例编号 06 → 07；
   新增 `3221225786` 退出码释义
3. 更新 `定时任务总览\任务清单.json`（14.8 KB，v1.1，校验通过）：新增 06 号；
   全部任务的 last_run/last_result/next_run 刷新到 2026-09-17 08:05；
   新增顶层 `cross_task_couplings`（4 条跨任务耦合）

### WorkBuddy 签到脚本的关键事实（写文档时确认）
- **登录态是明文 JSON**，`%LOCALAPPDATA%\CodeBuddyExtension\Data\Public\auth\workbuddy-desktop.info`，
  字段 `auth.accessToken` / `account.uid` / `auth.domain` / `auth.refreshToken` / `auth.expiresAt`
  → **不需要任何解密**，这是它比 Trae（AES-128-CBC 自解密）、TeleAgent（DPAPI + AES-256-GCM）都简单的原因
- 接口：`POST https://copilot.tencent.com/billing/meter/checkin-activity-status`（查）
  + `POST …/billing/meter/daily-checkin`（领），body `{}`
  → 可复用为「模板」写新的签到脚本
- 请求头：`Authorization: Bearer <token>` / `X-User-Id: <uid>` / `X-Domain`（有才带）
- 响应字段：`today_checked_in` / `streak_days` / `today_credit` / `daily_credit` / `active`
- 每日 100 分；实测日志 `2026/9/17 04:32:33 今日已签到。连续 2 天，今日积分 100，每日可领 100`
- ⚠ **借 TeleAgent 的 Node 运行时**（`~\.local\share\TeleAgent\runtimes\node\node.exe`）
  → 跨任务耦合：卸载 TeleAgent 会连带弄坏 WorkBuddy 签到。已写进 README 与总览的「隐藏依赖」
- ⚠ 登录态**明文存 token**，文档里已加"不要外传/不要进 Git"的提醒

### 顺带复核的 6 个任务状态（2026-09-17 08:05）
| 任务 | 上次运行 | 结果 | 下次 |
|---|---|---|---|
| 新闻联播每日总结 | 09-17 05:00:02 | `0` | 09-18 05:00 |
| 每日签到 | 09-17 00:10:01 | `0` | 09-18 00:10 |
| TeleAgent积分签到 | 09-17 01:00:01 | `0` | 09-18 01:00 |
| WorkBuddy积分签到 | 09-17 04:32:30 | `0` | 09-18 01:10 |
| XinWenLianBo_Sync_0600 | 09-17 06:00:01 | `0` | 09-18 06:00 |
| XinWenLianBo_Sync_30min | 09-17 07:57:01 | **3221225786** | 09-17 08:27 |

### 两个值得记的发现
1. **01 号首次自动运行成功**：09-17 05:00:02 触发、退出码 0，05:00:22 产出
   `归档\2026年9月\新闻联播总结_20260916.md`（44,640 B = 43.6 KB）。
   → "每天 05:00 直接跑 xwlb_report.py 生成昨天报告"这条链路**端到端跑通**，
   报告体积也落在预期区间（33 KB 均值，50 KB 才算异常）。
2. **05 号 07:57 那次返回 `3221225786`**（= `0xC000013A` `STATUS_CONTROL_C_EXIT`，进程被外部中止）。
   已排除的可能：仓库工作区干净（`git status --short` 空）、`main` 与 `origin/main` 同步、
   无残留 `.git\index.lock`、`Get-WinEvent System` 近 4 小时无休眠/唤醒事件、开机时间仍是 09-12。
   → **未造成数据损失**，原因未定，已作为"待观察项"写进 README 与 json。
   注意：TaskScheduler/Operational 日志未启用，取不到任务级事件，无法进一步定位。
   若后续频繁出现，优先怀疑：与 04 号撞车、杀软拦截、进程被强制结束。

### 环境经验（新增）
- `Get-WinEvent -LogName "Microsoft-Windows-TaskScheduler/Operational"` → **日志未启用**，
  报"找不到任何与指定的选择条件匹配的事件"。想查任务级事件需先启用该日志。
- 任务设置里 `StopIfGoingOnBatteries = True`（默认），本机无电池不影响，但换笔记本要注意。
