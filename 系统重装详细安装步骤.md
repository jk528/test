# 系统重装详细安装步骤

> 适用系统：Windows 10 Pro
> 生成日期：2026-08-31
> 总览：共 **7 个阶段**，约 **60+ 项**软件及配置

---

## 目录

- [阶段零：重装前准备](#阶段零重装前准备)
- [阶段一：系统基础配置](#阶段一系统基础配置)
- [阶段二：开发工具链](#阶段二开发工具链)
- [阶段三：数据库与容器](#阶段三数据库与容器)
- [阶段四：命令行工具与效率工具](#阶段四命令行工具与效率工具)
- [阶段五：日常软件](#阶段五日常软件)
- [阶段六：AI 与编程辅助工具](#阶段六ai-与编程辅助工具)
- [阶段七：个性化配置与收尾](#阶段七个性化配置与收尾)
- [附录：快速检查清单](#附录快速检查清单)

---

## 阶段零：重装前准备

### 0.1 数据备份（必做！）

| 备份项目 | 备份位置 / 命令 | 说明 |
|----------|-----------------|------|
| 桌面文件 | `C:\Users\Administrator\Desktop\` | 复制到 D 盘或移动硬盘 |
| 文档 | `C:\Users\Administrator\Documents\` | 同上 |
| 下载 | `C:\Users\Administrator\Downloads\` | 清理后只留有用的 |
| 浏览器书签 | Chrome：设置 → 书签 → 导出书签 | 保存为 HTML 文件 |
| SSH 密钥 | `C:\Users\Administrator\.ssh\` | **务必备份** |
| Git 配置 | `C:\Users\Administrator\.gitconfig` | 含用户名邮箱等 |
| VS Code 扩展列表 | `code --list-extensions > vscode-extensions.txt` | 导出扩展清单 |
| Python 依赖 | `pip freeze > requirements.txt` | 分别在 3.12 和 3.13 下执行 |
| npm 全局包 | `npm list -g --depth=0 > npm-global.txt` | - |
| 字体 | `C:\Windows\Fonts\` | 自行安装的字体需备份 |
| 微信聊天记录 | 微信 → 设置 → 聊天 → 聊天记录迁移 | 迁移到手机或备份 |
| 输入法词库 | 微信输入法 → 设置 → 词库 → 导出 | - |

### 0.2 记录当前配置

```powershell
# 导出已安装软件清单（运行后保存输出）
Get-ItemProperty "HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*" |
  Where-Object { $_.DisplayName -ne $null } |
  Select-Object DisplayName, DisplayVersion, Publisher |
  Sort-Object DisplayName |
  Export-Csv -Path "C:\backup\installed-software.csv" -NoTypeInformation

# 导出 PATH 环境变量
$env:PATH -split ';' | Out-File "C:\backup\path-env.txt"
```

### 0.3 准备安装介质

- [ ] Windows 10 Pro 安装 U 盘（或 ISO 文件）
- [ ] 主板/显卡/网卡驱动（提前下载到 U 盘）
- [ ] 常用软件安装包（可先下好放 U 盘，也可以装完后 winget 装）

---

## 阶段一：系统基础配置

### 1.1 安装 Windows 10 Pro

1. 插入安装 U 盘，从 U 盘启动
2. 选择「现在安装」→ 输入密钥（或先跳过）
3. 版本选择 **Windows 10 Pro**
4. 安装类型选「自定义：仅安装 Windows（高级）」
5. 选择系统盘，开始安装（过程会自动重启数次）
6. 初始设置：
   - 区域：中国
   - 键盘：微软拼音
   - 跳过网络连接（选「我没有 Internet 连接」→「继续执行受限设置」）
   - 创建本地账户：用户名 `Administrator`，设置密码
7. 进入桌面后，先检查设备管理器，确认驱动是否齐全

### 1.2 安装驱动（按优先级）

```
1. 主板芯片组驱动 → 重启
2. 显卡驱动（NVIDIA / AMD / Intel）→ 重启
3. 网卡驱动（有线 + 无线）
4. 声卡驱动
5. 其他外设驱动（打印机、扫描仪等）
```

> 💡 所有驱动装完后，设备管理器中不应有黄色感叹号。

### 1.3 Windows Update

1. 设置 → 更新和安全 → Windows 更新 → 检查更新
2. 安装所有可用更新，**反复检查直到没有更新为止**（可能需要重启多次）
3. 可选更新中安装驱动更新

### 1.4 系统基础设置

| 设置项 | 操作 |
|--------|------|
| 显示设置 | 调整分辨率、缩放比例 |
| 电源和睡眠 | 设置合适的睡眠时间 |
| 通知和操作 | 关闭不需要的通知 |
| 存储感知 | 开启，自动清理临时文件 |
| 远程桌面 | 如需远程，开启远程桌面 |
| 开发者选项 | 开启「开发人员模式」 |
| 任务栏 | 取消不需要的固定图标 |
| 开始菜单 | 关闭「偶尔在开始菜单中显示建议」 |
| 隐私 | 关闭广告 ID、位置、相机等不需要的权限 |

### 1.5 安装运行库

#### VC++ 运行库（必装）

很多软件依赖，务必先装：

```
下载地址：
- VC++ 2008 SP1 Redistributable (x64 + x86)
  https://www.microsoft.com/en-us/download/details.aspx?id=26368
- VC++ 2015-2022 Redistributable (x64 + x86)
  https://aka.ms/vs/17/release/vc_redist.x64.exe
  https://aka.ms/vs/17/release/vc_redist.x86.exe
```

安装顺序：先装 2008，再装 2015-2022。

#### DirectX 修复

```
1. 下载 DirectX Repair Tool 4.0
2. 解压后以管理员身份运行 DirectX Repair.exe
3. 点击「检测并修复」，等待完成
4. 修复完成后重启电脑
```

#### .NET Framework

- Windows 10 自带 .NET Framework 4.8，一般够用
- 如需旧版本（3.5）：控制面板 → 程序 → 启用或关闭 Windows 功能 → 勾选 .NET Framework 3.5

### 1.6 启用 Windows 功能

控制面板 → 程序 → 启用或关闭 Windows 功能：

- [x] Windows Subsystem for Linux（阶段三会用到）
- [x] 虚拟机平台（Docker/WSL2 需要）
- [x] Telnet 客户端（可选，调试用）
- [ ] Internet Explorer 11（不需要可以取消）

设置完后重启电脑。

---

## 阶段二：开发工具链

### 2.1 Git

```powershell
# 方式一：winget 安装（推荐）
winget install Git.Git

# 方式二：官网下载
# https://git-scm.com/download/win

# 安装选项注意：
# - 编辑器：选 VS Code 或 Vim
# - 默认分支名：main
# - PATH 环境：选 "Git from the command line and also from 3rd-party software"
# - 换行符：选 "Checkout Windows-style, commit Unix-style line endings"
# - 终端模拟器：选 MinTTY 或 Windows 默认控制台
```

安装后配置：

```powershell
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
git config --global init.defaultBranch main
git config --global core.autocrlf true

# 验证
git --version
```

### 2.2 GitHub CLI

```powershell
winget install GitHub.cli

# 验证
gh --version

# 登录 GitHub
gh auth login
# 按提示选择 GitHub.com → HTTPS → 登录浏览器授权
```

### 2.3 Visual Studio Build Tools 2026

> 提供 MSVC 编译器、Windows SDK 等，Rust 和很多 C/C++ 项目需要。

```powershell
# 下载安装引导程序
# https://visualstudio.microsoft.com/zh-hans/visual-cpp-build-tools/

# 或 winget 安装
winget install Microsoft.VisualStudio.2022.BuildTools

# 安装时勾选工作负载：
# ☐ 使用 C++ 的桌面开发（必选，含 MSVC、Windows SDK、CMake）
# ☐ .NET 桌面开发（可选）
```

安装完成后验证：在「x64 Native Tools Command Prompt for VS 2022」中运行 `cl`，能看到编译器版本。

### 2.4 CMake

```powershell
# 方式一：winget 安装（如已通过 VS Build Tools 安装可跳过）
winget install Kitware.CMake

# 方式二：官网下载
# https://cmake.org/download/

# 安装时选 "Add CMake to the system PATH for all users"

# 验证
cmake --version
```

### 2.5 Ninja

```powershell
winget install Ninja-build.ninja

# 验证
ninja --version
```

### 2.6 Python 3.12

```powershell
# 官网下载安装包
# https://www.python.org/downloads/release/python-31210/

# 或 winget
winget install Python.Python.3.12

# 安装时注意勾选：
# ☐ Add Python 3.12 to PATH
# ☐ Install for all users（可选，装到 C:\Program Files）
# 点击 Customize installation → 全选下一步 → 勾选：
#   ☐ Install for all users
#   ☐ Add Python to environment variables
#   ☐ Precompile standard library
```

验证：

```powershell
py -3.12 --version
pip --version
```

### 2.7 Python 3.13

```powershell
winget install Python.Python.3.13

# 安装选项同上
```

验证：

```powershell
py -3.13 --version
```

> 💡 Python Launcher（py 命令）会自动安装，通过 `py -3.12` 和 `py -3.13` 切换版本。

### 2.8 Node.js

```powershell
# 官网下载 LTS 版本
# https://nodejs.org/

# 或 winget
winget install OpenJS.NodeJS.LTS

# 安装时默认选项即可，会自动添加到 PATH
```

验证：

```powershell
node --version
npm --version
```

配置 npm 全局路径（可选，避免 C 盘占用）：

```powershell
npm config set prefix "D:\nodejs\npm-global"
npm config set cache "D:\nodejs\npm-cache"
# 然后把 D:\nodejs\npm-global 加到 PATH
```

### 2.9 Java (OpenJDK 21 - Temurin)

```powershell
winget install EclipseAdoptium.Temurin.21.JDK

# 或官网下载
# https://adoptium.net/

# 安装时勾选：
# ☐ Add to PATH
# ☐ Set JAVA_HOME variable
# ☐ JavaSoft (Oracle) registry keys
```

验证：

```powershell
java -version
javac -version
echo $env:JAVA_HOME
```

### 2.10 Rust

```powershell
# 官网下载 rustup-init.exe
# https://www.rust-lang.org/tools/install

# 运行后选 1（默认安装）
# 或使用 winget
winget install Rustlang.Rustup

# 安装完成后重启终端，验证
rustc --version
cargo --version
rustup --version
```

> 💡 Rust 依赖 MSVC，所以必须先装 VS Build Tools（步骤 2.3）。
> 如果安装慢，可以设置国内镜像：
> ```powershell
> $env:RUSTUP_DIST_SERVER = "https://mirrors.ustc.edu.cn/rust-static"
> $env:RUSTUP_UPDATE_ROOT = "https://mirrors.ustc.edu.cn/rust-static/rustup"
> ```

### 2.11 LLVM

```powershell
winget install LLVM.LLVM

# 或官网下载
# https://releases.llvm.org/

# 安装时勾选 "Add LLVM to the system PATH for all users"
```

验证：

```powershell
clang --version
```

### 2.12 Neovim

```powershell
winget install Neovim.Neovim

# 验证
nvim --version
```

可选配置：

```powershell
# 创建配置目录
mkdir ~/AppData/Local/nvim

# 如果有备份的 init.vim / init.lua，复制过去
```

### 2.13 Vim

```powershell
winget install vim.vim

# 验证（安装后需要重启终端）
vim --version
```

### 2.14 Visual Studio Code

```powershell
winget install Microsoft.VisualStudioCode

# 或官网下载
# https://code.visualstudio.com/

# 安装时勾选：
# ☐ 添加到 PATH（重启后生效）
# ☐ 其他按默认
```

安装扩展：

```powershell
# 基础扩展
code --install-extension ms-ceintl.vscode-language-pack-zh-hans   # 中文语言包
code --install-extension github.copilot-chat                       # Copilot Chat

# 开发相关扩展（可选推荐）
code --install-extension ms-python.python                          # Python
code --install-extension rust-lang.rust-analyzer                   # Rust
code --install-extension dbaeumer.vscode-eslint                    # ESLint
code --install-extension esbenp.prettier-vscode                    # Prettier
code --install-extension ms-vscode.cpptools                        # C/C++
code --install-extension redhat.java                               # Java
code --install-extension ms-azuretools.vscode-docker               # Docker
code --install-extension ckolkman.vscode-postgres                  # PostgreSQL
```

验证：

```powershell
code --version
```

---

## 阶段三：数据库与容器

### 3.1 PostgreSQL 16

```powershell
winget install PostgreSQL.PostgreSQL.16

# 或官网下载
# https://www.postgresql.org/download/windows/

# 安装时注意：
# - 安装路径：默认 C:\Program Files\PostgreSQL\16\
# - 数据目录：默认 C:\Program Files\PostgreSQL\16\data\
# - 设置 postgres 用户密码（务必记住！）
# - 端口：默认 5432
# - 勾选 Stack Builder（可选，用于安装附加组件）
```

验证：

```powershell
psql --version

# 测试连接
psql -U postgres
# 输入密码后进入 PostgreSQL 命令行
# \q 退出
```

配置环境变量（如果安装时没自动加）：

```
C:\Program Files\PostgreSQL\16\bin
C:\Program Files\PostgreSQL\16\lib
```

### 3.2 Redis

```powershell
# Redis 官方不支持 Windows，使用微软维护的版本
# 下载地址：https://github.com/microsoftarchive/redis/releases
# 下载 Redis-x64-3.0.504.msi

# 或 winget（如果有的话）
# winget install Microsoft.OpenJS.NodeJS  # 不对，redis winget 源可能不同
```

安装步骤：
1. 运行 msi 安装包
2. 勾选 "Add Redis to the PATH environment variable"
3. 设置端口（默认 6379）
4. 勾选 "Install Redis as a Service"（作为服务运行）
5. 设置内存限制（可选）

验证：

```powershell
redis-cli --version
redis-cli ping
# 应返回 PONG
```

### 3.3 WSL 2

> 阶段一已勾选 Windows 功能，这里安装发行版。

```powershell
# 查看可用发行版
wsl --list --online

# 安装 Ubuntu（推荐）
wsl --install -d Ubuntu

# 安装完成后重启电脑
# 第一次启动 Ubuntu 会让你设置用户名和密码
```

验证：

```powershell
wsl --list --verbose
# 应显示 Ubuntu，STATE 为 Running，VERSION 为 2
```

### 3.4 Docker Desktop

```powershell
winget install Docker.DockerDesktop

# 或官网下载
# https://www.docker.com/products/docker-desktop/

# 安装前确保已启用 WSL 2 和虚拟机平台（阶段一已做）
# 安装时勾选 "Use WSL 2 instead of Hyper-V"
```

安装后：
1. 启动 Docker Desktop
2. 同意服务条款
3. 进入设置 → Resources → WSL Integration → 开启 Ubuntu 集成
4. 应用并重启

验证：

```powershell
docker --version
docker run hello-world
```

> 💡 如果下载镜像慢，可以配置国内镜像源：
> 设置 → Docker Engine → 添加 registry-mirrors 配置。

---

## 阶段四：命令行工具与效率工具

### 4.1 fd（快速 find 替代）

```powershell
winget install sharkdp.fd

# 验证
fd --version
```

### 4.2 fzf（命令行模糊搜索）

```powershell
winget install junegunn.fzf

# 验证
fzf --version
```

### 4.3 jq（JSON 处理）

```powershell
winget install jqlang.jq

# 验证
jq --version
```

### 4.4 ripgrep（快速 grep 替代）

```powershell
winget install BurntSushi.ripgrep.MSVC

# 验证
rg --version
```

### 4.5 WinSCP（SFTP/FTP 客户端）

```powershell
winget install WinSCP.WinSCP

# 或官网下载
# https://winscp.net/
```

---

## 阶段五：日常软件

### 5.1 浏览器

#### Google Chrome

```powershell
winget install Google.Chrome

# 安装后登录 Google 账号同步书签、扩展、密码等
```

#### Microsoft Edge

```
系统自带，无需安装。
登录微软账号同步数据。
```

#### 夸克

```powershell
winget install Quark.QuarkBrowser

# 或官网下载
# https://www.quark.cn/
```

### 5.2 办公软件

#### Microsoft Office LTSC 2024 专业增强版

```
使用 Office 部署工具（ODT）安装：

1. 下载 Office Deployment Tool
   https://www.microsoft.com/en-us/download/details.aspx?id=49117

2. 解压到 C:\ODT

3. 创建配置文件 Configuration.xml（内容如下）：

<Configuration>
  <Add OfficeClientEdition="64" Channel="PerpetualVL2024">
    <Product ID="ProPlus2024Volume" PIDKEY="你的KMS密钥">
      <Language ID="zh-cn" />
      <ExcludeApp ID="Lync" />
      <ExcludeApp ID="Groove" />
    </Product>
  </Add>
  <Property Name="AUTOACTIVATE" Value="1" />
</Configuration>

4. 管理员运行命令提示符：
   cd C:\ODT
   setup.exe /configure Configuration.xml

5. 等待安装完成
```

> 或者使用 Office Tool Plus 等工具一键安装。

#### WPS Office

```powershell
winget install Kingsoft.WPSOffice

# 或官网下载
# https://www.wps.cn/
```

#### EdrawMind（亿图脑图）

```powershell
winget install EdrawSoft.EdrawMind

# 或官网下载
# https://www.edrawsoft.cn/mindmaster/
```

### 5.3 通讯软件

#### 微信

```powershell
winget install Tencent.WeChat

# 或官网下载
# https://pc.weixin.qq.com/
```

#### 微信输入法

```powershell
winget install Tencent.WeType

# 或官网下载
# https://z.weixin.qq.com/
```

#### 千问

```powershell
# 官网下载
# https://qianwen.aliyun.com/

# 或 Microsoft Store 搜索 "千问" 安装
```

### 5.4 云盘

#### 百度网盘

```powershell
winget install Baidu.BaiduNetdisk

# 或官网下载
# https://pan.baidu.com/download
```

#### 天翼云盘

```powershell
# 官网下载
# https://cloud.189.cn/download_client.jsp
```

### 5.5 下载工具

#### 迅雷

```powershell
winget install XunLei.Thunder

# 或官网下载
# https://www.xunlei.com/
```

### 5.6 压缩工具

#### 360压缩

```powershell
winget install 360.360Zip

# 或官网下载
# https://yasuo.360.cn/
```

> 💡 也可以选择更轻量的 7-Zip：
> ```powershell
> winget install 7zip.7zip
> ```

---

## 阶段六：AI 与编程辅助工具

### 6.1 TraeCode

```
官网下载：
https://www.trae.ai/

下载 Windows 版本安装包，运行安装即可。
安装后登录账号。
```

### 6.2 TraeWork

```
同 TraeCode 官网下载 TraeWork 版本。
https://www.trae.ai/
```

### 6.3 GitHub Copilot

```
VS Code 扩展已在阶段二安装（github.copilot-chat）。

激活：
1. 打开 VS Code
2. 点击左下角账户图标 → 登录 GitHub
3. 确保 GitHub 账号有 Copilot 订阅
4. Copilot 图标变亮表示激活成功
```

---

## 阶段七：个性化配置与收尾

### 7.1 还原备份数据

- [ ] 桌面文件复制回 `C:\Users\Administrator\Desktop\`
- [ ] 文档复制回 `C:\Users\Administrator\Documents\`
- [ ] SSH 密钥复制到 `C:\Users\Administrator\.ssh\`
- [ ] `.gitconfig` 复制到用户目录
- [ ] 浏览器导入书签
- [ ] VS Code 配置（`%APPDATA%\Code\User\`）
- [ ] Neovim 配置（`%LOCALAPPDATA%\nvim\`）

### 7.2 配置环境变量

按需要添加以下到系统或用户 PATH：

```
用户级 PATH 参考：
C:\Users\Administrator\.cargo\bin
C:\Users\Administrator\AppData\Local\Programs\Python\Python312\Scripts\
C:\Users\Administrator\AppData\Local\Programs\Python\Python312\
C:\Users\Administrator\AppData\Local\Programs\Python\Python313\Scripts\
C:\Users\Administrator\AppData\Local\Programs\Python\Python313\
C:\Users\Administrator\AppData\Local\Programs\Python\Launcher\
C:\Users\Administrator\.local\bin
C:\Users\Administrator\AppData\Roaming\npm
C:\Users\Administrator\AppData\Local\Microsoft\WinGet\Links
```

系统 PATH 一般安装时自动添加，检查是否有：

```
系统级 PATH 参考：
C:\Program Files\Common Files\Oracle\Java\javapath
C:\Program Files\Microsoft VS Code\bin
C:\Program Files\CMake\bin
C:\Program Files\nodejs\
C:\Program Files\Git\cmd
C:\Program Files\GitHub CLI\
C:\Program Files\Neovim\bin
C:\Program Files\Docker\Docker\resources\bin
C:\Program Files\Redis\
C:\Program Files\LLVM\bin
C:\Program Files\PostgreSQL\16\bin
```

### 7.3 安装 npm 全局包

```powershell
npm install -g @marp-team/marp-cli
npm install -g cheerio
npm install -g docx
npm install -g pptxgenjs
npm install -g undici
```

验证：

```powershell
npm list -g --depth=0
```

### 7.4 安装 Python 包

```powershell
# Python 3.12
py -3.12 -m pip install --upgrade pip
py -3.12 -m pip install csvkit beautifulsoup4 camelot-py fonttools

# 如果你有 requirements.txt
py -3.12 -m pip install -r requirements.txt

# Python 3.13
py -3.13 -m pip install --upgrade pip
```

### 7.5 终端美化（可选）

#### 安装 Windows Terminal

```powershell
winget install Microsoft.WindowsTerminal
```

#### 安装 PowerShell 7

```powershell
winget install Microsoft.PowerShell
```

#### 安装 Oh My Posh

```powershell
winget install JanDeDobbeleer.OhMyPosh

# 然后在 PowerShell $PROFILE 中添加：
# oh-my-posh init pwsh --config "$env:POSH_THEMES_PATH/jandedobbeleer.omp.json" | Invoke-Expression
```

### 7.6 最终检查

在 PowerShell 中运行以下命令，验证所有工具是否就绪：

```powershell
Write-Host "=== 开发工具检查 ==="
git --version
gh --version
python --version   # 或 py -3.12 --version
node --version
npm --version
java -version 2>&1 | Select-Object -First 1
javac -version
rustc --version
cargo --version
clang --version
cmake --version
ninja --version
code --version | Select-Object -First 1
nvim --version | Select-Object -First 1
docker --version

Write-Host "=== 数据库检查 ==="
psql --version
redis-cli --version

Write-Host "=== 命令行工具 ==="
fd --version
fzf --version
jq --version
rg --version

Write-Host "=== 包管理器 ==="
winget --version
pip --version
cargo --version
```

全部输出版本号而不报错，说明安装成功。

### 7.7 创建系统还原点

```
1. 控制面板 → 系统 → 系统保护
2. 选中 C 盘 → 配置 → 启用系统保护 → 调整磁盘空间 → 确定
3. 点击「创建」→ 输入名称（如 "刚装完所有软件"）→ 创建
4. 等待完成
```

---

## 附录：快速检查清单

装完一项打个勾，避免遗漏：

### 系统基础
- [ ] Windows 10 Pro 安装完成
- [ ] 驱动全部安装（设备管理器无感叹号）
- [ ] Windows Update 全部更新
- [ ] VC++ 2008 + 2015-2022 运行库
- [ ] DirectX 修复
- [ ] WSL + 虚拟机平台 已启用
- [ ] 系统基础设置完成

### 开发工具
- [ ] Git + 配置
- [ ] GitHub CLI + 登录
- [ ] VS Build Tools 2026 (C++ 桌面开发)
- [ ] CMake
- [ ] Ninja
- [ ] Python 3.12
- [ ] Python 3.13
- [ ] Node.js + npm
- [ ] OpenJDK 21 (Temurin)
- [ ] Rust (rustup)
- [ ] LLVM (clang)
- [ ] Neovim
- [ ] Vim
- [ ] VS Code + 扩展

### 数据库与容器
- [ ] PostgreSQL 16
- [ ] Redis
- [ ] WSL 2 + Ubuntu
- [ ] Docker Desktop

### 命令行工具
- [ ] fd
- [ ] fzf
- [ ] jq
- [ ] ripgrep
- [ ] WinSCP

### 浏览器
- [ ] Google Chrome
- [ ] Microsoft Edge（自带）
- [ ] 夸克

### 办公软件
- [ ] Office LTSC 2024 专业增强版
- [ ] WPS Office
- [ ] EdrawMind

### 通讯软件
- [ ] 微信
- [ ] 微信输入法
- [ ] 千问

### 云盘 / 下载 / 压缩
- [ ] 百度网盘
- [ ] 天翼云盘
- [ ] 迅雷
- [ ] 360压缩

### AI 工具
- [ ] TraeCode
- [ ] TraeWork
- [ ] GitHub Copilot

### 收尾
- [ ] 数据还原（桌面/文档/SSH/浏览器书签等）
- [ ] 环境变量配置
- [ ] npm 全局包
- [ ] Python 包
- [ ] 最终检查通过
- [ ] 创建系统还原点

---

> 祝重装顺利！🎉
