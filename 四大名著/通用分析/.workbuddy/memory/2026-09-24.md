# 2026-09-24 工作日志

## 白描方法论 v2：首轮口径改为「全集」

- 文件：`拆分文本白描逐回分析_通用任务方法论.md`（工作区根目录，已改为 v2）
- 需求：用户要求首轮全片段即**全集**，不要 v1 的「精选 2–4 条 + 补全轮」两轮制。
- 主要改动：
  1. 判定口径 §2.3 改为「命中 = 片段，首轮即全集，一一对应（允许合条）」；
  2. 步骤 3 首轮约束改为写全命中手法，并**强制分段落盘**（每 3–6 章一次，先覆写后 Edit 追加）；
  3. 步骤 8「补全轮」降级为「补漏轮（仅异常时启用）」；§0.3 角色同步改名；
  4. 模板 A 重写（新增「## 五、写入方式」段与全集口径硬要求）；模板 B 改为补漏模板；
  5. §4.2 新增「输出侧」批次预算（min 读入侧/输出侧，落 [6,12]）；建议 8–12 章/批；
  6. §6.2 校验脚本加反向越界检查；§7 增两条红线（精选惯性回潮、批次过载）；
  7. §8 实测数据重标注为 v1 口径参照，并新增「差集占比 59%」「全集口径建议批次」行；
  8. 修正原文交叉引用 bug：空返回处置由「§7.3」改为正确的「§7.7」。
- 安全动作：原件已备份至 `.temp/bak/拆分文本白描逐回分析_通用任务方法论.orig.md`（25947 字节，与原件一致）。
- 落地校验：无 BOM、LF 换行保持、16074 字符（原 12966）；旧版模板句「每…选2至4个最典型」已清零。
- 同目录其他文件已回扫：全工程仅此一份方法论，无跨文档需同步（三国/水浒/红楼梦的分析产出属 v1 结果，模板不反向引用本文）。

## 经验（已并入技能 win-safe-cn-doc-edit）

- 整篇重写（改动 > 全文 1/3）优于几十条锚点替换：先备份 → Write 重建 → Python 回读三查（编码换行 / 新串有旧串无 / 首尾哨兵）。
- 若新版里保留了旧措辞（如「v1→v2 变更摘要」表），断言要挑**只在旧版上下文出现的完整句**，不能只看关键词。
