---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '3a8ff728-f35f-4330-b5de-662c7b3a1e45'
  PropagateID: '3a8ff728-f35f-4330-b5de-662c7b3a1e45'
  ReservedCode1: 'eb73f21c-8534-4e3c-9071-2fa4eb1b3a46'
  ReservedCode2: 'eb73f21c-8534-4e3c-9071-2fa4eb1b3a46'
---

# 四平台积分签到 API 直连 —— 逆向研究总集

> 汇总日期：2026-09-19
> 覆盖范围：TraeWork / Trae CN / TeleAgent / WorkBuddy 四个平台的签到功能逆向
> 用途：一份文档涵盖所有平台的逆向思路、加密方案、接口清单、踩坑记录和排障指南
> 配套文件：各平台独立研究文档 + `Electron应用API逆向通用指南.md` + `checkin_all.js` 统一脚本

---

## 一、四平台总览

### 1.1 基本信息对照

| 维度 | TraeWork | Trae CN | TeleAgent | WorkBuddy |
|------|----------|---------|-----------|-----------|
| 产品方 | 字节跳动 | 字节跳动 | 中电信人工智能 | 腾讯 |
| 进程名 | TRAE SOLO CN.exe | Trae CN.exe | TeleAgent.exe | WorkBuddy.exe |
| 客户端版本 | 1.107.1 | 1.107.1 | 2.5.2 | 2.5.2 |
| 代码打包 | 未打包（明文 JS） | 未打包（明文 JS） | app.asar（297MB） | app.asar（470MB） |
| 定时任务 | TraeWork积分签到 00:10 | TraeCN积分签到 01:20 | TeleAgent积分签到 01:00 | WorkBuddy积分签到 01:10 |
| 每日积分 | ~200（含额外50） | ~200（含额外50） | 100 | 100 |

### 1.2 加密方案对照

| 维度 | TraeWork | Trae CN | TeleAgent | WorkBuddy |
|------|----------|---------|-----------|-----------|
| token 格式 | Base64 加密 | 同 TraeWork | Base64 → v10 OSCrypt | **明文 JSON** |
| 加密算法 | AES-128-CBC | 同 TraeWork | AES-256-GCM | 无 |
| 密钥派生 | SHA-512 + XOR 内置常量 | 同 TraeWork | DPAPI（Windows 用户级） | 无 |
| 解密难度 | 中 | 中 | 中 | **零** |
| 解密依赖 | 纯 JS | 纯 JS | DPAPI + Node crypto | 无 |

### 1.3 API 对照

| 维度 | TraeWork | Trae CN | TeleAgent | WorkBuddy |
|------|----------|---------|-----------|-----------|
| API 网关 | api.trae.cn | api.trae.cn | agent.teleai.com.cn | copilot.tencent.com |
| 签到方法 | POST | POST | GET | POST |
| 鉴权头 | Cloud-IDE-JWT | 同 TraeWork | X-Token | Bearer JWT |
| 风控参数 | req_source=2 + 数字设备ID | 同 TraeWork | 无签名 | Turing Token（可选） |
| 客户端依赖 | 不需要 | 不需要 | 不需要 | 不需要 |

### 1.4 TraeWork 与 Trae CN 的特殊关系

两者是**独立产品**（不同安装目录、不同数据目录），但签到 API **完全共用**：
- 同一域名 `api.trae.cn`
- 同一接口路径 `/trae/api/v2/ug/checkin_credits/status|claim`
- 同一加密方案（AES-128-CBC + SHA-512 + 内置常量）
- 唯一区别：数据目录（`TRAE SOLO CN` vs `Trae CN`）

---

## 二、各平台逆向要点

### 2.1 TraeWork（TRAE SOLO CN）

**数据目录**：`%APPDATA%\TRAE SOLO CN\`

**token 存储**：
- 文件：`User\globalStorage\storage.json`
- 键名：`iCubeAuthInfo://icube.cloudide`
- 格式：Base64 加密字符串

**解密链路**：
```
Base64 解码 → 提取 key(32字节) → SHA-512(key) → XOR(ure,dre)
→ 拼接 [SHA-512(key) + XOR] → SHA-512 → 取前16字节=AES密钥, 16-32=IV
→ AES-128-CBC 解密 → 去掉前64字节 → 明文 JSON {token, userRegion}
```

**接口**：
- POST `https://api.trae.cn/trae/api/v2/ug/checkin_credits/status` body=`{"req_source":2}`
- POST `https://api.trae.cn/trae/api/v2/ug/checkin_credits/claim` body=`{"req_source":2}`

**鉴权头**：`Authorization: Cloud-IDE-JWT <token>` + `x-device-id` + `x-device-type` + `x-os-version` + `x-app-version`

**风控踩坑**：
- `req_source` 必须为 2（桌面端标识），缺失报 code 9004
- `x-device-id` 必须为纯数字格式（如 `1963102595468793`），用 UUID 格式报 code 9074

> 详细文档：`TraeWork积分API直连-逆向研究全记录.md`

---

### 2.2 Trae CN

**数据目录**：`%APPDATA%\Trae CN\`

**与 TraeWork 的差异**：仅数据目录不同，其余完全一致。解密代码直接复用 TraeWork 的 `decryptTrae()` 函数。

**额外踩坑**：
- `product.json` 中配置的域名是 `api.trae.com.cn`，但签到 API 实际在 `api.trae.cn`（前者返回 404）
- 需要多域名测试才能发现正确域名

> 详细文档：`TraeCN积分API直连-逆向研究全记录.md`

---

### 2.3 TeleAgent

**数据目录**：`%USERPROFILE%\.local\share\TeleAgent\`

**token 存储**：
- 文件：`users\<uid>\app-auth\token.json` → `encryptedToken`（Base64）
- 主密钥：`Local State` → `os_crypt.encrypted_key`（DPAPI 加密）
- 设备 ID：`app-auth\device-meta.json` → `deviceId`

**解密链路**：
```
1. Local State → os_crypt.encrypted_key → Base64 解码 → 去掉 "DPAPI" 前缀
2. DPAPI Unprotect (CurrentUser) → 32 字节 AES 主密钥
3. encryptedToken → Base64 解码 → "v10" + nonce(12) + ciphertext + tag(16)
4. AES-256-GCM 解密 → JWT token (439字符)
```

**接口**：
- GET `https://agent.teleai.com.cn/superCowork/sapi/user/portal/companyStore/v2/checkDayTask`
- GET `https://agent.teleai.com.cn/superCowork/sapi/user/portal/companyStore/checkTask?type=daily_login`
- GET `https://agent.teleai.com.cn/superCowork/sapi/user/portal/companyStore/userCenter`

**鉴权头**：`X-Token` + `X-Timestamp` + `X-Nonce` + `X-SuperAgent-Device-Id` + `X-OS-Type`

**关键发现**：无请求签名，头齐全即过。

> 详细文档：`TeleAgent积分API直连-逆向研究全记录.md`

---

### 2.4 WorkBuddy（CodeBuddy）

**数据目录**：
- 主数据：`%USERPROFILE%\.workbuddy\`
- **auth 文件不在 `.workbuddy` 下**，在 `%LOCALAPPDATA%\CodeBuddyExtension\Data\Public\auth\`

**token 存储**：
- 文件：`workbuddy-desktop.info`
- 格式：**明文 JSON**（无需解密）
- 字段：`auth.accessToken`（JWT）、`account.uid`、`auth.domain`

**发现 token 的曲折过程**：
```
.restore() → getAuthSavePath() → sharedDataPath → basePath → EXTENSION_DATA_DIR_NAME
→ "CodeBuddyExtension" → %LOCALAPPDATA%\CodeBuddyExtension\Data\Public\auth\
```

**接口**：
- POST `https://copilot.tencent.com/billing/meter/checkin-activity-status` body=`{}`
- POST `https://copilot.tencent.com/billing/meter/daily-checkin` body=`{}`

**鉴权头**：`Authorization: Bearer <token>` + `X-User-Id`

**风控发现**：Turing Token（腾讯防刷）是可选增强，`postCheckin` 代码逻辑为"无头也照发"，实测确认 200 通过。

> 详细文档：`WorkBuddy积分API直连-逆向研究全记录.md`

---

## 三、四平台 token 路径速查

| 平台 | token 文件 | 键名/字段 | 格式 |
|------|----------|---------|------|
| TraeWork | `%APPDATA%\TRAE SOLO CN\User\globalStorage\storage.json` | `iCubeAuthInfo://icube.cloudide` | Base64 加密 |
| Trae CN | `%APPDATA%\Trae CN\User\globalStorage\storage.json` | `iCubeAuthInfo://icube.cloudide` | Base64 加密 |
| TeleAgent | `%USERPROFILE%\.local\share\TeleAgent\users\<uid>\app-auth\token.json` | `encryptedToken` | v10 OSCrypt |
| TeleAgent 主密钥 | `%USERPROFILE%\.local\share\TeleAgent\Local State` | `os_crypt.encrypted_key` | DPAPI |
| WorkBuddy | `%LOCALAPPDATA%\CodeBuddyExtension\Data\Public\auth\workbuddy-desktop.info` | `auth.accessToken` | 明文 JSON |

| 平台 | 设备 ID 文件 | 格式 |
|------|------------|------|
| TraeWork | `%APPDATA%\TRAE SOLO CN\ModularData\ckg_server\local_env.json` | 纯数字 |
| Trae CN | `%APPDATA%\Trae CN\ModularData\ckg_server\local_env.json` | 纯数字 |
| TeleAgent | `%USERPROFILE%\.local\share\TeleAgent\app-auth\device-meta.json` | 十六进制 MAC |
| WorkBuddy | 不需要 | - |

---

## 四、四平台接口完整清单

### TraeWork / Trae CN（共用）

网关：`https://api.trae.cn`

| 用途 | 方法 + 路径 | 请求体 | 鉴权头 |
|------|------------|--------|--------|
| 签到状态 | POST `/trae/api/v2/ug/checkin_credits/status` | `{"req_source":2}` | `Cloud-IDE-JWT` + `x-device-id` 等 |
| 领取积分 | POST `/trae/api/v2/ug/checkin_credits/claim` | `{"req_source":2}` | 同上 |

### TeleAgent

网关：`https://agent.teleai.com.cn/superCowork/sapi`

| 用途 | 方法 + 路径 | 鉴权头 |
|------|------------|--------|
| 签到状态 | GET `/user/portal/companyStore/v2/checkDayTask` | `X-Token` + `X-Timestamp` + `X-Nonce` + `X-SuperAgent-Device-Id` + `X-OS-Type` |
| 领取积分 | GET `/user/portal/companyStore/checkTask?type=daily_login` | 同上 |
| 积分余额 | GET `/user/portal/companyStore/userCenter` | 同上 |

### WorkBuddy

网关：`https://copilot.tencent.com`

| 用途 | 方法 + 路径 | 请求体 | 鉴权头 |
|------|------------|--------|--------|
| 活动状态 | POST `/billing/meter/checkin-activity-status` | `{}` | `Bearer` + `X-User-Id` |
| 签到状态 | POST `/billing/meter/checkin-status` | `{}` | 同上 |
| 领取积分 | POST `/billing/meter/daily-checkin` | `{}` | 同上 |

---

## 五、跨项目踩坑总集

### 5.1 通用坑（多项目重复遇到）

| # | 问题 | 涉及项目 | 解决 |
|---|------|---------|------|
| 1 | rg 搜索 asar 报 "binary file matches" | TeleAgent / WorkBuddy | 加 `-a` 强制文本模式 |
| 2 | 搜 `points/checkin` 上万条命中 | TeleAgent / WorkBuddy | 搜 UI 独有中文字符串当锚点 |
| 3 | PS 5.1 `ConvertFrom-Json` 解析大 JSON 失败 | TeleAgent | 用 Node 解析 |
| 4 | asar 被进程占用无法 `File.Open` | WorkBuddy | 用 Node `fs.openSync('r')` 或 rg |
| 5 | PS 修改 JS 文件后 Node 报语法错误 | TeleAgent | PS 5 的 `Set-Content` 默认 ANSI，用 Write 工具 |
| 6 | 日志中文乱码 | 全部 | `Get-Content -Encoding UTF8` 读取 |
| 7 | 定时任务注册成功但不执行 | 全部 | `Test-Path "C:\Windows\System32\Tasks\任务名"` 验证落盘 |

### 5.2 各平台特有坑

| 平台 | 坑 | 解决 |
|------|---|------|
| TraeWork | `req_source` 缺失报 9004 | 请求体固定 `{"req_source": 2}` |
| TraeWork | 设备 ID 用 UUID 报 9074 | 用 `local_env.json` 里的纯数字 `device_id` |
| TraeWork | 解密后 JSON 前 64 字节是垃圾 | `dec.slice(64)` 去掉（rh=64） |
| Trae CN | `product.json` 配的 `api.trae.com.cn` 返回 404 | 实际域名是 `api.trae.cn`（与 TraeWork 一致） |
| TeleAgent | DPAPI 直接解 token 失败 | token 是 OSCrypt v10 格式，先解主密钥再 AES-GCM |
| TeleAgent | 密文头 `76 31 30` 不认识 | = ASCII "v10"，Chromium OSCrypt 版本前缀 |
| TeleAgent | asar JSON 起始偏移与网传不符 | hexdump 前 64 字节确认：JSON 从偏移 16 开始 |
| WorkBuddy | auth 文件不在 `.workbuddy` 下 | 逆向 `getAuthSavePath` 链路找到 `CodeBuddyExtension` |
| WorkBuddy | `EXTENSION_DATA_DIR_NAME` 不是 "WorkBuddy" | 底层是 CodeBuddy 体系，值为 "CodeBuddyExtension" |

---

## 六、统一脚本架构（checkin_all.js）

四个平台已集成为统一脚本，各平台独立执行，互不阻塞。

```
run_checkin_all.cmd → checkin_all.js
    │
    ├── 1. TraeWork 签到
    │   ├── 读取 %APPDATA%\TRAE SOLO CN\... \storage.json
    │   ├── AES-128-CBC 解密 token
    │   ├── POST status → POST claim → 刷新积分
    │   └── 随机延迟 30~300 秒
    │
    ├── 2. TraeCN 签到（复用解密函数）
    │   ├── 读取 %APPDATA%\Trae CN\... \storage.json
    │   ├── 同上解密 + API 调用
    │   └── 随机延迟 30~300 秒
    │
    ├── 3. TeleAgent 签到
    │   ├── DPAPI 解主密钥 → AES-256-GCM 解 token
    │   ├── GET status → GET claim → GET userCenter
    │   └── 随机延迟 30~300 秒
    │
    ├── 4. WorkBuddy 签到
    │   ├── 明文读取 auth JSON
    │   ├── POST status → POST claim → 复查
    │   └── 随机延迟 30~300 秒
    │
    └── 汇总输出：成功 N/4，失败 N/4
```

设计要点：
- **随机延迟**：每个平台签到前等待 30~300 秒，打乱计划任务的固定启动时间
- **独立执行**：单平台失败不影响其他平台
- **统一日志**：带时间戳的中文输出，退出码 0=全部成功、1=至少一个失败
- **零安装**：Node 运行时借用 TeleAgent 自带（`%USERPROFILE%\.local\share\TeleAgent\runtimes\node\node.exe`）

---

## 七、逆向方法论总结

### 通用六步流程

```
界面字符串 → 渲染层代码 → API 路径 → 鉴权头 → token 来源 → 解密链路 → 验证接口
```

### 搜索锚点选择

| 策略 | 适合场景 | 例子 |
|------|---------|------|
| UI 独有中文 | 所有 Electron 应用 | 「积分福利社」「连续签到」「立即领取」 |
| i18n 键名 | 有多语言的应用 | `account.checkin.*` |
| API 路径片段 | 代码未混淆 | `/checkin_credits` `/daily-checkin` |
| 响应字段名 | 从 UI 数据反推 | `claimed_today` `daily_claimable_points` |

### 判断加密类型

| 密文特征 | 加密类型 | 解密方法 |
|---------|---------|---------|
| 文件是明文 JSON | 无加密 | 直接 `JSON.parse`（WorkBuddy） |
| Base64 开头 `v10` | Chromium OSCrypt | DPAPI 解主密钥 → AES-256-GCM（TeleAgent） |
| Base64 + 内置常量表 | 自研混淆 | 逆向解密函数（TraeWork/TraeCN） |

> 更完整的通用指南见 `Electron应用API逆向通用指南.md`

---

## 八、配套文件清单

| 文件 | 说明 |
|------|------|
| `checkin_all.js` | 统一签到脚本（4 平台集成） |
| `run_checkin_all.cmd` | 统一签到入口 |
| `TraeWork积分API直连-逆向研究全记录.md` | TraeWork 独立研究文档 |
| `TraeCN积分API直连-逆向研究全记录.md` | Trae CN 独立研究文档 |
| `TeleAgent积分API直连-逆向研究全记录.md` | TeleAgent 独立研究文档 |
| `WorkBuddy积分API直连-逆向研究全记录.md` | WorkBuddy 独立研究文档 |
| `Electron应用API逆向通用指南.md` | 通用逆向方法论 |
| `TeleAgent与Trae签到实现对比.md` | 早期对比文档（已被本文件取代） |
| **本文件** | 四平台逆向总集 |

---

*本文档汇总自 2026-09-16 至 2026-09-19 期间的四平台逆向研究成果。*

> AI生成