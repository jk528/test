# 白描批次还原与格式统一记录（2026-09-24）

## 一、背景

09-24 的目录整理把 `输入法相关\.temp\` 整体作为「临时文件」清理（送回收站）。
该目录实为四大名著「白描手法逐回分析」的**批次中间产物工作区**，非普通缓存。
用户要求：**把白描批次 .md + hits.txt 还原到四大名著对应文件夹**。

## 二、可还原性核查

| 项 | 结果 |
|---|---|
| 回收站实体 | `C:\$Recycle.Bin\S-1-5-21-...\$RCPU7BK.temp` |
| 实体状态 | **存在，目录** |
| 实测内容 | **118 文件 / 2,509,351 字节** |
| `$I` 索引记录 size | 2,509,351 字节 |
| 一致性 | **一字节不差** → 判定完整可还原，无需动用 git 兜底 |

> 附带修正：整理时报告所称「67 个白描批次 .md」为粗估。逐个数过后实为
> **75 个 .md + 4 个 hits.txt**，另有 39 个 `_*.py` / `_*.txt` 分析探针脚本。

## 三、还原范围

**79 个文件 = 75 个 .md + 4 个 hits.txt**。
39 个探针脚本未还原，仍完整保留在回收站中，随时可再取。

## 四、落点结构（终态）

```
语料与文本\四大名著\
├─ 红楼梦\红楼梦分析\白描批次\      13
├─ 三国演义\三国演义分析\白描批次\  13
├─ 水浒传\水浒传分析\白描批次\      13
└─ 西游记\西游记分析\白描批次\      13
```

**四部书均为完全相同的 13 个文件、同一命名**：

| 文件 | 说明 |
|---|---|
| `batch_01.md` ~ `batch_10.md` | 逐回白描分析批次（每 10~12 回一卷） |
| `head.md` / `tail.md` | 全书首 / 尾片段 |
| `hits.txt` | 逐回命中手法清单 |

## 五、执行结果

| 项 | 数值 |
|---|---|
| 计划复制 | 79 |
| 成功 | **79** |
| 失败 | **0** |
| 目标同名冲突 | **0** |
| 校验方式 | 逐文件 `size` + `MD5` 双重比对 |
| 回收站原件 | **保留未动**（copy 而非 move） |

## 六、格式与命名统一（09-24 11:40 追加）

用户要求「hits.txt 都仿照红楼梦」。核查发现四份命中清单当时并不一致：

| 文件 | 当时差异 | 处理 |
|---|---|---|
| `红楼梦\hits.txt` | 基准：UTF-8 无 BOM / LF / `N\|手法,手法` | **未动**（作为基准） |
| `三国演义\hits.txt` | 多一个 UTF-8 BOM + CRLF 换行 | 去 BOM、CRLF → LF |
| `水浒传\hits.txt` | 多一个 UTF-8 BOM + CRLF 换行 | 去 BOM、CRLF → LF |
| `西游记\hits.txt` | 格式为中文式 `第N回：手法、手法` | **100 行全部改写**为 `N\|手法,手法` |

**命名同步统一**（西游记侧原带 `xyj_` 前缀）：

| 原文件名 | 现文件名 |
|---|---|
| `xyj_hits.txt` | `hits.txt` |
| `xyj_head.md` | `head.md` |
| `xyj_tail.md` | `tail.md` |

**引用修正 4 处**：`《西游记》白描手法逐回解析：100回…md` 1 处、`白描批次\tail.md` 1 处、
本记录、项目日志。复扫确认**零残留旧文件名引用**。

**格式规范依据**：`四大名著\通用分析\拆分文本白描逐回分析_通用任务方法论.md`
第 149 / 224 行明确定义 ——

```
hits.txt                          # 全书命中数据，每行：章号|手法A,手法B,…
```

红楼梦那份正是符合该规范的产物；西游记原格式属偏离。

**终态校验**：四份 hits.txt 全部 UTF-8 无 BOM、LF 换行、每一行均通过
`^\d+\|[^|]+(,[^|]+)*$` 校验。行数：红楼梦 / 三国演义 / 水浒传 各 **120 行**，
西游记 **100 行**（回数不同，属正常）。

## 七、与既有成果的关系

各书 `X分析\` 目录下已有的 **`《X》白描手法逐回解析：120回叙事技巧统合分析.md`**
（西游记为 100 回）正是这批批次的**下游汇总产物**。批次归位到其同级 `白描批次\`
子目录，两者并列、互不覆盖。

## 八、后续清理与恢复方式

还原后用户在 09-24 11:33–11:36 做了一轮自主清理，以下均已进回收站、可还原：

| 被清理项 | 位置 |
|---|---|
| `三国演义白描分析总结.txt` | `三国演义\三国演义分析\` |
| `水浒传白描分析总结.txt` | `水浒传\水浒传分析\` |
| `西游记白描手法命中清单_hits.txt` | `西游记\西游记分析\`（与 `hits.txt` **MD5 完全相同**，属纯副本） |
| `_xyj_task_brief.md` | `西游记\西游记分析\白描批次\` |
| `_parts\`（21 个 a/b 拆分件） | `西游记\西游记分析\白描批次\` |
| `_bak\`（5 个旧版批次） | `西游记\西游记分析\白描批次\` |

本次还原为**复制**操作，原始数据仍在回收站。若需撤销，直接删除四个 `白描批次\` 目录即可。
