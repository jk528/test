# JK-temp 整理报告

**执行时间**：2026-09-24
**工作区**：`C:\Users\Administrator\Documents\这是什么\JK-temp`
**执行范围**：A 类可再生/临时文件清理 + B 类旧版文件清理 + 顶层目录主题归类

---

## 一、总览

| 指标 | 整理前 | 整理后 | 变化 |
|---|---|---|---|
| 文件数 | 36,347 | 4,375 | **−31,972** |
| 工作区体积 | 9.83 GB | 2.07 GB | **−7.76 GB（−79%）** |
| 顶层目录数 | 14 个（含 `.git`） | 5 个分组 + `.git` | 结构归并 |
| C 盘可用空间 | 3.13 GB | 3.13 GB | **未变（见 §6）** |

清理量分布：

| 类别 | 体积 | 处理方式 |
|---|---|---|
| 构建产物（可再生） | 7,656.50 MB | 经环境安全通道送入回收站 |
| 临时 / 逆向中间产物 | 48.77 MB | 送回收站 |
| 已取代的旧版文档 | 0.63 MB | 送回收站 |
| **合计** | **7,705.90 MB** | |

---

## 二、A 类：已清理明细

| 目标 | 体积 | 文件数 | 重建方式 |
|---|---|---|---|
| `红楼梦范例\app\src-tauri\target\` | 6,037.84 MB | 6,025 | `cargo build` / `pnpm tauri dev` |
| `彩读分析\.temp\ColorTxt\node_modules\` | 1,398.76 MB | 21,797 | `npm install` |
| `红楼梦范例\app\node_modules\` | 206.14 MB | 3,590 | `pnpm install` |
| `红楼梦范例\app\dist\` | 13.76 MB | 98 | `vite build` |
| `彩读分析\.temp\qoder\` | 27.30 MB | 17 | 逆向中间产物，不需重建 |
| `红楼梦范例\app\.temp\` | 17.77 MB | 233 | 探针脚本 / 截图 / 运行日志 |
| `输入法相关\.temp\` | 2.39 MB | 118 | 调查脚本 + 中间批次 |
| `四大名著\通用分析\.temp\` | 0.03 MB | 7 | 检查脚本 |
| `__pycache__\` × 11 处 | 0.63 MB | — | Python 自动生成 |
| `*.log` × 4 | 0.40 MB | 4 | 运行日志 |
| `~$*.docx` × 2 | 0.00 MB | 2 | Word 残留锁文件 |

> `.gitignore` 原本已声明 `**/src-tauri/target/`、`node_modules/`、`dist/` 不入库，
> 因此这些删除不影响 Git 内容。

---

## 三、B 类：**否决的删除项**（重要）

预检阶段用**逐文件全量 MD5** 复核，推翻了最初判断的两个删除项，已从清单撤下。

### ✗ 撤销：`新闻联播\流程\备份\归档_20260916\`

原判断为「`新闻联播\归档\` 的冗余备份」。全量比对结果：

```
备份侧 107 文件 / 正式侧 115 文件
内容相同 50 份  |  内容不同 57 份  |  仅正式侧独有 8 份
```

**57 份文件内容不同**，属历史版本快照而非冗余副本，删除会真实丢失数据 → **保留**。

> 根因：最初的重复检测只比较「文件大小 + 首 1MB 哈希」，无法发现同尺寸不同正文的情况。

### ✗ 撤销：`红楼梦.txt` 与 `红楼梦_拆分/` 的两份副本

`四大名著\红楼梦\` 与 `红楼梦范例\` 各持一份，均为 120 文件全量一致（MD5 逐份比对）。但两者**各自被不同生态活跃引用**：

- `红楼梦范例\红楼梦_拆分\` ← `skill\V3.4分析任务说明书.md`、`脚本工具\*.py`、全部 V3.x 分析报告
- `四大名著\红楼梦\红楼梦_拆分\` ← `四大名著\通用分析\splittxt` 的输入源，与其余三部名著的拆分目录构成完整一套

删除任一侧都会破坏真实引用，节省仅 5 MB → **两侧均保留**。

### ✓ 实际执行的 B 类清理

| 目标 | 体积 | 文件数 | 理由 |
|---|---|---|---|
| `红楼梦范例\分析结果\之前的\` | 0.44 MB | 24 | V2 期旧报告，已被 V3.0–V3.4 存档取代 |
| `红楼梦范例\skill\之前的\` | 0.19 MB | 14 | 旧版模板，已被现行模板取代 |

两者均受 Git 追踪，必要时可用 `git checkout` 从历史恢复。

---

## 四、顶层目录归类结果

```
阅读器项目\        红楼梦范例 · 彩读分析 · ColorTxt项目分析
语料与文本\        四大名著 · 小说 · 红楼梦基础元素
自动化与工具\      新闻联播 · VBA转js · 输入法相关
环境与配置\        Gitee同步配置 · ERT
```

> **后续调整（2026-09-24 11:50）**
> - `test1.xlsm` 已按用户要求**移回根目录**（`JK-temp\test1.xlsm`），不再归入 `阅读器项目\`。
>   移动采用「复制 → MD5 校验 → 删源」，`git hash-object` 与 `HEAD:test1.xlsm` 一致（`ae639889…`），
>   内容零改动，`git status` 已恢复为无变更状态。
> - `创作与写作\`（同人小说可行性分析）与 `环境与配置\ERT\ERT培训.html` 于 11:01–11:05 由用户自行删除；
>   `新闻联播\流程\归档` 于 11:43 由用户自行删除（经核实非计划任务输出路径，日报不受影响）。
>   故顶层当前为 **4 个分组**。

| 分组 | 体积 | 文件数 |
|---|---|---|
| 语料与文本 | 952.96 MB | 776 |
| 阅读器项目（本次测量含归入的 `test1.xlsm`，后已移回根目录） | 254.74 MB | 1,991 |
| 自动化与工具 | 9.62 MB | 360 |
| 创作与写作 | 1.24 MB | 32 |
| 环境与配置 | 0.35 MB | 15 |

（另有 `.git/` 904.23 MB 保持不动）

搬迁采用同卷 `os.rename`（仅改元数据，不复制数据）。

---

## 五、引用修正

搬迁前全量扫描，共发现 **7 个文件 15 处**真实跨目录路径引用（其余 "小说"、"INSERT" 等为词义误匹配）。

### 已自动修正（52 个文件）

| 修正类型 | 说明 | 影响文件数 |
|---|---|---|
| `JK-temp\<目录>` → `JK-temp\<分组>\<目录>` | 绝对路径，正反斜杠两种写法 | 45 |
| `file:///c:/.../JK-temp/<目录>/` 链接 | 文档内可点击链接 | 含上 |
| `.gitignore` 锚定规则 × 3 | `彩读分析/.temp/ColorTxt/`、`/红楼梦范例/install_*.log` | 1 |
| 手写相对路径 | `../红楼梦范例/…` → `../../阅读器项目/红楼梦范例/…` 等 | 7 |

涉及的关键文件：
- `红楼梦范例\脚本工具\` 下 **18 个 .py**（写死的 `project_dir` / `output_dir` / `split_dir`）
- `红楼梦范例\skill\` 规范文档中的 `PROJECT_ROOT`
- `VBA转js\代码延伸\` 下 7 个 .md 的 `file:///` 链接
- `彩读分析\` 下 6 个 .md 的项目路径与代码链接
- `四大名著\通用分析\章节正则统合.md`、`app\src\lib\chapters.ts` 的来源注释

**修正后回扫：未发现任何未修正的旧路径引用。**

### 已修正的外部引用

| 对象 | 修正前 | 修正后 |
|---|---|---|
| Windows 计划任务「新闻联播每日总结」 | `…\JK-temp\新闻联播\流程\一键生成\xwlb_report.py` | `…\JK-temp\自动化与工具\新闻联播\流程\一键生成\xwlb_report.py` |
| 同上 WorkingDirectory | 同上 | 同上 |
| 桌面 `红楼梦阅读分析.lnk` | 指向旧 `红楼梦范例\` | 已重建，指向 `阅读器项目\红楼梦范例\` |
| 桌面 `彩读分析.lnk` | 指向旧 `彩读分析\` | 已重建，指向 `阅读器项目\彩读分析\` |

计划任务状态已确认为 `Ready`，下次触发可正常执行。

> 未受影响：桌面 `JK-temp.lnk`（指向仓库根）、`彩读.lnk`（指向已安装的 `colortxt\彩读.exe`）、`彩读找书.lnk`。

启动器脚本（`启动红楼梦阅读分析.bat/.vbs`、`启动彩读分析.vbs`、`start_colortxt.bat`）全部使用 `%~dp0` / `WScript.ScriptFullName` 相对定位，**无需修改**。

---

## 六、⚠️ 磁盘空间：需要你决策

C 盘空间**没有真正释放**，原因如下：

1. 本次清理目标为「送回收站」，回收站实体位于 `C:\$Recycle.Bin`，占用的是 C 盘空间；
2. C 盘当时仅剩 **3.13 GB** 空闲，且该卷回收站配额上限约 819 MB，**7.5 GB 构建产物根本放不进回收站**；
3. 环境的安全删除层将删除操作统一转为回收站操作，因此数据从工作区移动到了回收站，**净空间变化为零**。

**当前状态**：

- 工作区：9.83 GB → 2.12 GB
- 回收站：**6.16 GB**（约 10,488 项，含本次全部清理内容）
- C 盘可用：3.13 GB

**要真正腾出这 6.16 GB，需要清空回收站中本次产生的条目。** 请告知是否执行——我会只清除本次产生的条目，不影响回收站中原有的其它内容。

---

## 七、遗留事项

| # | 事项 | 说明 |
|---|---|---|
| 1 | 两个空壳目录（均 0 项） | `JK-temp\彩读分析\` 与 `JK-temp\.temp\`。内容已全部清空，但目录本身被某进程作为工作目录/监听目标占用，`rename`/`rmdir` 均返回 WinError 32。**重启一次即可自动删除**，或手动删除。不影响任何功能。 |
| 2 | Git 尚未提交 | `git status` 显示 2,350 项 `D` + 1 项 `M`。新位置的文件尚未 `git add`，因此 Git 暂将其识别为「旧路径删除」。执行 `git add -A` 后 Git 会自动识别为重命名。**需要我提交吗？** |
| 3 | 其余可选瘦身项 | `新闻联播\流程\旧版本\`（354 KB）、`四大名著\通用分析\残留\`（0.06 MB）、`小说\` 中的 52 个 zip（203 MB，经核验均为唯一副本，**不建议删**） |

---

## 八、恢复方式

- **回收站内文件**：在资源管理器中打开回收站，按原路径还原（`$I` 索引记录了完整原始路径），或使用我提供的按项还原。
- **Git 追踪文件**：`git checkout -- <路径>`（需在提交前执行）。
- **构建产物**：
  ```bash
  cd 阅读器项目/红楼梦范例/app && pnpm install && pnpm tauri dev   # 重建 node_modules + target
  cd 阅读器项目/彩读分析/.temp/ColorTxt && npm install               # 重建 ColorTxt 依赖
  ```

---

## 九、执行过程中的技术验证记录

| 检查项 | 手段 | 结果 |
|---|---|---|
| 重复文件识别 | 全量 MD5（逐 1MB 分块） | 195 组重复；据此否决 2 项误判删除 |
| 删除前快照 | 逐目标统计体积/文件数 | A 类 7,705.27 MB / 31,933 项 |
| 回收站入站确认 | 解析 `C:\$Recycle.Bin\*\ $I` 索引 | 原始路径完整可还原 |
| 删除结果判定 | 以「路径消失」为准 | ⚠️ `SHFileOperationW` 返回码不可信（成功时返回 2） |
| 文本改写安全 | 先验证可编码再写盘 + 回读断言 | 52 个文件全部通过，无 latin-1 回写 |
| 长路径 | 全树最长 252 字符（<260） | 无超限 |
| 进程占用 | 枚举 node/electron/cargo/app | 无项目进程运行 |
