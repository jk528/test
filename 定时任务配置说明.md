# PC端Git同步定时任务配置说明

## 文件说明

| 文件名 | 说明 |
|--------|------|
| `auto-sync.bat` | Windows批处理同步脚本（英文输出，避免编码问题） |
| `auto-sync-v2.ps1` | PowerShell同步脚本（UTF-8编码，支持中文） |
| `sync.log` | 同步日志文件 |

## 脚本功能

1. 检查本地 JK-temp 目录是否有新文件或修改
2. 如果有未跟踪的文件或修改，执行 `git add . && git commit -m "定时同步: $(date)"`
3. 推送到 Gitee: `git push gitee-test main:main`
4. 推送到 GitHub: `git push github main:main`
5. 如果没有改动，记录日志"无变更，跳过同步"

## 特性

- ✅ 支持中文文件名
- ✅ 网络超时自动重试（最多3次，每次间隔5秒）
- ✅ 完整的日志记录
- ✅ 错误处理和状态报告

## Windows定时任务配置步骤

### 方法一：使用任务计划程序（推荐）

1. **打开任务计划程序**
   - 按 `Win + R`，输入 `taskschd.msc`，回车

2. **创建基本任务**
   - 点击右侧"创建基本任务"
   - 名称：`GitAutoSync`
   - 描述：`自动同步JK-temp到Gitee和GitHub`

3. **设置触发器**
   - 选择触发器类型（如"每天"、"每小时"等）
   - 设置开始时间和重复间隔

4. **设置操作**
   - 选择"启动程序"
   - 程序/脚本：`C:\Users\Administrator\Documents\这是什么\JK-temp\auto-sync.bat`
   - 起始于：`C:\Users\Administrator\Documents\这是什么\JK-temp`

5. **完成设置**
   - 勾选"打开属性对话框"
   - 点击"完成"

6. **高级设置（可选）**
   - 在属性对话框中：
     - "常规"选项卡：勾选"不管用户是否登录都要运行"
     - "条件"选项卡：根据需要设置网络条件
     - "设置"选项卡：设置任务失败后的重试次数

### 方法二：使用命令行创建

以管理员身份运行PowerShell，执行以下命令：

```powershell
# 创建每天执行的任务（上午9点）
$action = New-ScheduledTaskAction -Execute "C:\Users\Administrator\Documents\这是什么\JK-temp\auto-sync.bat" -WorkingDirectory "C:\Users\Administrator\Documents\这是什么\JK-temp"

$trigger = New-ScheduledTaskTrigger -Daily -At "09:00"

$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable

Register-ScheduledTask -TaskName "GitAutoSync" -Action $action -Trigger $trigger -Settings $settings -Description "自动同步JK-temp到Gitee和GitHub"
```

### 方法三：每小时同步一次

```powershell
# 创建每小时执行的任务
$action = New-ScheduledTaskAction -Execute "C:\Users\Administrator\Documents\这是什么\JK-temp\auto-sync.bat" -WorkingDirectory "C:\Users\Administrator\Documents\这是什么\JK-temp"

# 每小时执行一次
$trigger = New-ScheduledTaskTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Hours 1) -RepetitionDuration (New-TimeSpan -Days 365)

$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable

Register-ScheduledTask -TaskName "GitAutoSync-Hourly" -Action $action -Trigger $trigger -Settings $settings -Description "每小时自动同步JK-temp到Gitee和GitHub"
```

## 手动测试

在PowerShell中执行：

```powershell
cd "C:\Users\Administrator\Documents\这是什么\JK-temp"
.\auto-sync.bat
```

## 日志查看

同步日志保存在：`C:\Users\Administrator\Documents\这是什么\JK-temp\sync.log`

查看日志：
```powershell
Get-Content "C:\Users\Administrator\Documents\这是什么\JK-temp\sync.log" -Tail 50
```

## 常见问题

### 1. 中文文件名乱码
- 脚本已设置UTF-8编码
- 如仍有乱码，请确保Git配置正确：`git config --global core.quotepath false`

### 2. 推送超时
- 脚本已内置重试机制（3次，间隔5秒）
- 如网络不稳定，可增加重试次数或延长超时时间

### 3. 权限问题
- 确保任务计划程序以具有Git仓库访问权限的用户运行
- 如需推送，确保已配置好SSH密钥或HTTPS凭据

## 远程仓库配置检查

确保本地仓库已配置好远程仓库：

```bash
git remote -v
```

应显示：
```
gitee-test  https://gitee.com/username/repo.git (fetch)
gitee-test  https://gitee.com/username/repo.git (push)
github      https://github.com/username/repo.git (fetch)
github      https://github.com/username/repo.git (push)
```

## 修改同步频率

如需修改定时任务的执行频率：

1. 打开任务计划程序
2. 找到 `GitAutoSync` 任务
3. 右键 -> 属性 -> 触发器 -> 编辑
4. 修改重复间隔

---

**注意**：首次配置建议先手动运行脚本测试，确保配置正确后再设置定时任务。
